# CURRENT_STATUS.md - PracticeIQ

Last updated: 2026-05-05 (post Section 14 Step 3C closure, C-2026-05-03-01; Pre-3D Product Architecture & SaaS Guardrail Scan adoption, C-2026-05-03-02; Governance File Maintenance & Independent Review Protocol adoption, C-2026-05-03-03; Cost Discipline at Stage 0 adoption, C-2026-05-03-04; External Threat Security & Platform Hardening Guardrails adoption, C-2026-05-03-05; Section 14 Step 3D-1 push and deploy, C-2026-05-04-01; post-3D-1 deployment sync, C-2026-05-04-02; Section 14 Step 3D-2 push and deploy, C-2026-05-04-03; post-3D-2 deployment sync, C-2026-05-04-04; Section 14 Step 3D-3 push and deploy, C-2026-05-04-05; post-3D-3 deployment sync, C-2026-05-04-06; Step 3 checkpoint audit + governance touchup, C-2026-05-04-07; pre-3E permissions matrix touchup, C-2026-05-05-01; Section 14 Step 3E-1 push and deploy, C-2026-05-05-02; post-3E-1 deployment sync, C-2026-05-05-03; pre-3E-2A governance touchup, C-2026-05-05-04; Section 14 Step 3E-2A push and deploy, C-2026-05-05-05; post-3E-2A deployment sync, C-2026-05-05-06; Section 14 Step 3E-2B push and deploy, C-2026-05-05-07; post-3E-2B deployment sync, C-2026-05-05-08; Section 14 reorder approval (Step 4 before 3F), C-2026-05-06-01; Section 14 Step 4A Auth architecture confirmation, C-2026-05-06-02; Section 14 Step 4B-1 package/env/helper preparation drafted, C-2026-05-06-03; Section 14 Step 4B-1 push, deploy, and Netlify-verified (commit `eb6dbc9`), C-2026-05-06-04; Section 14 Step 4B-2 push, deploy, and Netlify-verified (commit `0c47cd7`), C-2026-05-06-05; Section 14 Step 4D push, deploy, and Netlify-verified (commit `1b88f80`), C-2026-05-06-06; Section 14 Step 4E push, deploy, and Netlify-verified (commit `92a73f2`), C-2026-05-06-07; and Section 14 Step 4E-FU firm-route audit call-site follow-on push, deploy, and Netlify-verified (commit `662126d`), C-2026-05-06-08)
Update rule: edit after every milestone, audit, or stage shift.

## Repo Health

- Branch: `main` (in sync with `origin/main`)
- Latest verified runtime/code commit: `662126d` (`Section 14 Step 4E-FU: Add firm route audit call sites`)
- Section 14 Step 4E-FU (firm-route audit call-site follow-on) pushed, deployed, and Netlify-verified live on 2026-05-06: full 17-endpoint regression matrix all green and identical to post-4E (expected — 4E-FU adds audit emit on the success path of `POST /api/firms` and `PATCH /api/firms/[firmId]`, which is invisible to unauthenticated traffic because the auth gate fires before the mutation runs). `GET /api/team`, `GET /api/team/dummy-id-for-401-check`, `GET /api/tasks`, `GET /api/activity`, `GET /api/clients` all return 401 with the standard `{"ok":false,"message":"Authentication required."}` envelope; `GET /api/team/dummy-id-for-401-check/deactivate` and `/reactivate` return 405. Step 4D firms/tenant gates remain in force: `POST /api/firms` (no auth) → 401, `PATCH /api/firms/dummy-id-for-401-check` (no auth) → 401, `POST /api/firms/dummy-id-for-401-check/access` → 410 with removed-endpoint message, `POST /api/firms/dummy-id-for-401-check/members` → 410 pointing to `POST /api/team`, `POST /api/tenant/validate` (valid body) → public 200 `{"ok":true}`. Netlify production deploy `69fb87f7e56dae000824ee16` (full commit ref `662126d03a9ee70d1d5271a087c1364169a3e1e8`) reached state `ready` at 18:27:57 UTC; 52-second build; plugin `success`; branch `main`; context `production`; 1 function deployed (`___netlify-server-handler` via `@netlify/plugin-nextjs@5.15.10`, runtime `nodejs20.x`, region `us-east-2`); scanned files count 62 (unchanged from `92a73f2` 4E deploy — confirms 4E-FU is a 2-file body-change addition, not new files). **Firm-route audit call-site follow-on shipped**: `POST /api/firms` now emits a `FIRM_CREATE` ActivityLog row on successful authenticated firm creation (firmId = new firm's id; actorId = session.userId; entityType = "FIRM"; entityId = new firm's id; metadataJson intentionally omitted — full details recoverable via Firm record join from entityId). `PATCH /api/firms/[firmId]` now emits a `FIRM_UPDATE` ActivityLog row on successful authenticated firm update (firmId = server-resolved session.firmId; actorId = session.userId; entityType = "FIRM"; entityId = updated.id; metadataJson = `{ fields: ["name","city","status","emailDomain"] }` — field names only, no values). Both call sites preserve the existing 4D auth gates exactly: `POST /api/firms` still requires PLATFORM_OWNER via inline platformRole check; `PATCH /api/firms/[firmId]` still requires `Action.FIRM_UPDATE` via `requireAuth(...)`; cross-firm hits on PATCH still return 404 with `console.warn` per Section 25.4 #15. **Privacy guardrails preserved**: no raw request body, no full firm object snapshot, no headers, no cookies, no tokens, no passwords, no secrets, no env vars, no IP, no user-agent. The free-text `name` and `city` operator inputs and the `emailDomain` value are NOT logged — only column names in PATCH; entityId reference only in POST. All identifiers are server-resolved cuids. Files shipped in `662126d` (2 files; 32 insertions / 2 deletions per cached diff stat): `src/app/api/firms/route.ts` (added `writeActivityLog` to imports; inserted `FIRM_CREATE` audit emit between `prisma.firm.create({...})` await and the success return), `src/app/api/firms/[firmId]/route.ts` (added `writeActivityLog` to imports; inserted `FIRM_UPDATE` audit emit between `prisma.firm.update({...})` await and the success return). **No `src/lib/api-helpers.ts` change** — the 4E `writeActivityLog()` implementation is consumed unchanged. **No new permission action** introduced. **No auth/RBAC behaviour changed.** No route added or removed. No response-shape change; existing success envelopes preserved (`{ ok: true, firm: {...} }` shape unchanged for both routes). **No schema/migration/package/env/config/Netlify/Supabase setting changed.** **Externally observable behaviour at Stage 0 is unchanged** because the 200 / audit-write path requires an authenticated Supabase session (no users seeded; no login UI). With 4E-FU deployed and verified, **Step 4E and its firm-route audit follow-on are now closed**; the audit pipeline is comprehensive across all 15 mutation actions (13 from 4E + 2 from 4E-FU). Only Step 4F (audited PLATFORM_OWNER cross-firm impersonation flow per Section 25.6) and 4G (Auth UAT and tenant leakage testing) remain pending under Step 4. **Step 4F is the next controlled implementation sub-wave**.
- Live URL: `https://practice-iq.netlify.app/` (Netlify auto-deploys from GitHub `main`)
- Build: `npm run uat:check` passing per Pankaj's local verification (note: origin renamed `release:check` to `uat:check`)
- Five Netlify env vars set: `DATABASE_URL`, `DIRECT_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- Supabase project: provisioned in Mumbai (`ap-south-1`); first migration `20260429185225_init_postgres` applied
- **Stage gate**: Founder-led POC (Stage 0). Pre-real-client-data checklist not yet started. See MASTER_PROJECT.md Section 22 and D-2026-05-03-01.
- **Pre-3D guardrail scan adopted**: Section 14 Step 3D plan must consume MASTER_PROJECT.md Section 23 (TaskStatus / Priority canonical sets, transition matrix, reopen / cancel / closure rules, inactive user / client handling, cross-firm ID validation, audit event taxonomy, plan-tier feature codes, entitlement helper shape, Writing Assist deferred guardrails) as implementation constraints. See D-2026-05-03-02 and AGENTS.md G7.
- **Governance protocol active**: Pre-commit five-file consistency check applies from C-2026-05-03-03 forward. Independent ChatGPT review is a named control input at major Section 14 transitions. See MASTER_PROJECT.md Section 24 and AGENTS.md G8.
- **Cost discipline at Stage 0 active**: every recommendation that touches tooling, infrastructure, or spend uses the four-row template (Free / Paid / Recommendation / Trigger) per AGENTS G9. Detailed principle at MASTER_PROJECT.md Section 22.9. See D-2026-05-03-04.
- **Security guardrails active**: Section 14 Step 3D and onward route planning consumes MASTER_PROJECT.md Section 25 (route-construction checklist, task-route specifics, Step 4 requirements, RLS / DB requirements, platform hardening, monitoring, future module security, G9 cost-discipline matrix, CA / CPA client-trust lens, Platform Ownership Register status, consumption rule) alongside Section 23. AGENTS G7 effective scope is extended to include Section 25 by D-2026-05-03-05; no new G10 added. See D-2026-05-03-05.
- **Step 3D-1 pushed, deployed, and Netlify-verified**: Tasks foundation + read/create routes live on `https://practice-iq.netlify.app` since 2026-05-04 (commit `8754760`). `/api/tasks` and `/api/tasks/[id]` return 401 locked-by-default with the standard `{"ok":false,"message":"Authentication required."}` envelope. Decisions A through F locked at D-2026-05-04-01; Decisions G / H / I consumed from Section 25. See C-2026-05-04-01 for the implementation wave and C-2026-05-04-02 for this post-deployment doc-sync. 3D-2 (Tasks mutations) and 3D-3 (Tasks lifecycle actions) remain pending.
- **Step 3D-2 pushed, deployed, and Netlify-verified**: Tasks mutations live on `https://practice-iq.netlify.app` since 2026-05-04 (commit `13d8b4f`). PATCH `/api/tasks/[id]` (corrected permission flow with TASK_VIEW first-gate + operation classifier dispatching to TASK_EDIT or TASK_MOVE_TO_REVIEW per D-2026-05-04-02), POST `/api/tasks/[id]/notes` (status-less progress note), and PATCH `/api/tasks/[id]/assignees` (set-semantics add/remove with route-layer ARTICLE_STAFF rejection per Section 23.5). `/api/tasks` and `/api/tasks/[id]` still return 401 locked-by-default with the standard `{"ok":false,"message":"Authentication required."}` envelope; the new `/notes` and `/assignees` endpoints are registered in the build route table (POST and PATCH respectively; GET on either returns 405 Method Not Allowed which itself proves route registration). Decisions J1 / K1 / L1 locked at D-2026-05-04-02. No changes to `src/lib/permissions.ts` or `src/lib/task-constants.ts`. See C-2026-05-04-03 for the implementation wave and C-2026-05-04-04 for the post-deployment doc-sync. 3D-3 status now pushed, deployed, and Netlify-verified (see next bullet).
- **Step 3D-3 pushed, deployed, and Netlify-verified**: Tasks lifecycle actions live on `https://practice-iq.netlify.app` since 2026-05-04 (commit `8bcf4d1`). POST `/api/tasks/[id]/close`, POST `/api/tasks/[id]/reopen`, and POST `/api/tasks/[id]/cancel` registered in the build route table per the Netlify deploy of `8bcf4d1`. `/api/tasks`, `/api/tasks/[id]`, and `/api/activity` all still return 401 locked-by-default with the standard `{"ok":false,"message":"Authentication required."}` envelope; the new lifecycle endpoints accept POST only (GET on any of them returns 405 Method Not Allowed which itself proves route registration). Two new permission action codes added in 3D-3: `Action.TASK_REOPEN` and `Action.TASK_CANCEL`. Implements Decision M = M1 (visibility-then-permission for ARTICLE_STAFF on lifecycle endpoints) and the reopen field-clear correction (closure fields cleared on reopen — `closedAt`, `closedById`, `closureRemarks` all set to null) locked at D-2026-05-04-03. Also updated MASTER Section 23.6 `TASK_CLOSE` audit-metadata row to `{ noteId }` reference per Decision I alignment. Files shipped: `src/lib/permissions.ts` (REWRITE — added `Action.TASK_REOPEN`, `Action.TASK_CANCEL` + base array additions for FIRM_ADMIN / PARTNER + 2 context-aware rules in `hasPermission()`), `src/app/api/tasks/[id]/close/route.ts` (NEW), `src/app/api/tasks/[id]/reopen/route.ts` (NEW), `src/app/api/tasks/[id]/cancel/route.ts` (NEW). No `src/lib/task-constants.ts` change. With 3D-3 done, full Section 14 Step 3D (Tasks route group) is complete; pending route groups become `team/` (3E) and `modules/` (3F) only. See C-2026-05-04-05 for the implementation wave and C-2026-05-04-06 for this post-deployment doc-sync.
- **Step 3 checkpoint audit completed (post-3D close, pre-3E)**: Result **YELLOW** with five low-severity findings; zero critical issues, zero security defects, zero runtime risk. Audit run on committed state at runtime/code SHA `8bcf4d1` and doc-sync HEAD `6f8710e`. Findings: F1 (clients routes 3B lack `.strict()` on body schemas — governance-compliant per "from 3D onward" wording but pattern-divergent), F2 (clients routes 3B lack cross-firm `console.warn` per Section 25.4 #15 — same status as F1), F3 (Section 23.3 reopen prose did not mention `closureRemarks` clearing), F4 (Section 25.5 conflated edit/close permission requirements), F5 (Section 23.3 cancel wording said "any non-terminal state" but implemented behaviour rejects `CLOSED`). **F3, F4, F5 fixed in this touchup wave (C-2026-05-04-07)** — MASTER Section 23.3 reopen + cancel blocks and Section 25.5 permission wording rewritten to match approved code behaviour locked at D-2026-05-04-02 and D-2026-05-04-03. **F1 / F2 deferred** to Step 4 Auth hardening (which touches every route anyway) or a separate small clients-route cleanup wave. No code change in this touchup; code is the approved behaviour, docs are aligning to it. Section 14 Step 3D remains closed; 3E remains pending. See C-2026-05-04-07.
- **Pre-3E permissions matrix touchup drafted locally**: One-line code change to `src/lib/permissions.ts` adding `Action.TEAM_VIEW` to the FIRM_ADMIN base permission array (Path-1 selected: ship as a separate tiny pre-3E-1 commit rather than bundling into 3E-1 implementation). Discovered during the 3E-1 plan re-read on 2026-05-05; FIRM_ADMIN previously had `TEAM_MANAGE` but not `TEAM_VIEW` in the base array. The Step 3 checkpoint audit did not catch this because no current route consumes `TEAM_VIEW`. The matrix change is dormant until Step 4 wires real sessions AND 3E-1 routes ship; locked-by-default 401 contract is unaffected. **3E-1 decisions A1 / B1 / C1 / D1 / E1 approved**: A1 (ARTICLE_STAFF can see full firm team list including email), B1 (default-hide inactive members; `?status=active|inactive|all` filter), C1 (`q` search is name-only for 3E-1), D1 (create `src/lib/team-constants.ts` during 3E-1 implementation), E1 (this matrix touchup; Path-1). The matrix touchup landed in repo HEAD `96c57db`. See C-2026-05-05-01 and D-2026-05-05-01.
- **Step 3E-1 pushed, deployed, and Netlify-verified**: Team foundation + read routes live on `https://practice-iq.netlify.app` since 2026-05-05 (commit `caafcd2`, Netlify deploy `69f9580d6f2a9600082977c2`, state `ready`, published 02:38:52 UTC, 46-second build, plugin success). `GET /api/team` and `GET /api/team/[id]` return 401 locked-by-default with the standard `{"ok":false,"message":"Authentication required."}` envelope. `GET /api/tasks` and `GET /api/activity` regression checks remain 401. Implements approved decisions A1 (full firm team list including email visible to ARTICLE_STAFF), B1 (inactive hidden by default; `?status=active|inactive|all`), C1 (`q` search is name-only, case-insensitive substring match), D1 (`src/lib/team-constants.ts` shipped with `FIRM_ROLES`, `TEAM_STATUS_FILTERS`, `DEFAULT_TEAM_PAGE_SIZE = 50`, `MAX_TEAM_PAGE_SIZE = 200`, `DEFAULT_TEAM_STATUS_FILTER = "active"`). Response shape: `firmMemberId`, `userId`, `name`, `email`, `firmRole`, `isActive`, `joinedAt`. Excluded from response: `passwordHash`, `platformRole`, `PlatformUser.isActive`, `lastLoginAt`, `firmId`. `[id]` is `FirmMember.id` (not `PlatformUser.id`). Cross-firm 404 + `console.warn` on `/api/team/[id]` per Section 25.4 #15. PLATFORM_OWNER without firmId returns 400. Files shipped in `caafcd2`: `src/lib/team-constants.ts` (NEW), `src/app/api/team/route.ts` (NEW, GET only), `src/app/api/team/[id]/route.ts` (NEW, GET only). No POST / PATCH / deactivate / reactivate (3E-2 territory). No `src/lib/permissions.ts` change in this wave (matrix gap already fixed in `96c57db` per D-2026-05-05-01). No schema, package, or config change. With 3E-1 deployed and verified, only 3E-2 (team mutations) and 3F (modules) remain pending in Section 14 Step 3. Earlier `/api/team` 404 from a same-day live check was Netlify deploy lag (the check ran before the build at 02:38:52 UTC completed); not a route defect. See C-2026-05-05-02 for the implementation wave and C-2026-05-05-03 for this post-deployment doc-sync.
- **3F reorder question parked (D-2026-05-05-03)**: An alternative Section 14 sequence was proposed during the 2026-05-05 unattended block — defer 3F Modules until after Step 4 Auth/RBAC because module routes may touch Platform Owner cross-firm behaviour, entitlement gating, and impersonation-adjacent logic. **The reorder is NOT approved.** Default sequence (3D ✓ → 3E → 3F → Step 4 → Step 5) remains in force. 3E-2A continues as the next implementation wave; 3E-2B follows; 3F follows 3E-2B; Step 4 does not start automatically. Revisit trigger: after 3E is fully closed (3E-2A + 3E-2B both deployed and verified), before starting 3F planning, OR during Step 4 planning if a stronger argument for reorder surfaces. See D-2026-05-05-03 and C-2026-05-05-04.
- **Pre-3E-2A governance touchup completed (drafted locally)**: Documentation-only wave following the AI Advisory & Risk-Control Playbook audit on 2026-05-05. Three workstreams: (a) refreshed stale operational truth in `CURRENT_STATUS.md` ("What Is Partially Built", "What Is Missing", "Next 5 to 10 Priority Tasks" sections updated to reflect Tasks shipped + Team read shipped + correct pending list); (b) recorded the parked status of the 3F reorder question via D-2026-05-05-03 + the bullet above; (c) codified Playbook execution rules in `AGENTS.md` as new G10 (Claude handoff + PowerShell hygiene) and G11 (pre-major-wave stress test + conservative Stage 0 tenant defaults), recorded via D-2026-05-05-04. `MASTER_PROJECT.md` NOT edited in this wave per operator decision (no Section 26 Playbook; rules live in AGENTS instead). No code change, no schema change, no route change, no package / config / env change. Locked-by-default contract unaffected. Latest verified runtime/code commit (line above) NOT advanced — stays at `caafcd2`. See C-2026-05-05-04.
- **Step 3E-2A pushed, deployed, and Netlify-verified**: Team add/update routes live on `https://practice-iq.netlify.app` since 2026-05-05 (commit `f94027d`, Netlify deploy `69fa147c5346e80008ea84d8`, full commit ref `f94027d15894cb536524ce156dd0e65073427174`, state `ready`, published 16:03:00 UTC, 48-second build, plugin success, 1 new function asset uploaded which confirms runtime delta vs the earlier docs-only `c94ae1e` deploy). `GET /api/team` and `GET /api/team/dummy-id-for-401-check` return 401 locked-by-default with the standard `{"ok":false,"message":"Authentication required."}` envelope, confirming both POST gate (via `/api/team`) and PATCH gate (via `/api/team/[id]`) hold. `GET /api/tasks` and `GET /api/activity` regression checks remain 401. Implements approved decisions A1 + A-CORRECTION (POST creates `PlatformUser` + `FirmMember` in one transaction only when email is genuinely new; cross-firm existing `PlatformUser` returns 422 with generic message and `console.warn` for forensics; no silent multi-firm linking), D1 (PATCH carries name + firmRole; no dedicated /role endpoint), F1 role variant (self-role-change rejected with 422), G1 role variant (last-active-FIRM_ADMIN demotion rejected with 422), I1 (PlatformUser.email globally unique per schema), I1-NORMALIZE (email trim + lowercase before any DB lookup or store), J1 (sentinel-prefixed placeholder `passwordHash` of form `STEP4_MIGRATE_DISABLED:<v4-uuid>`), K1 (defer allowed-domain enforcement to Step 4), 3E-2-O1 (new PlatformUser always `platformRole = "STANDARD"`), D-3E-DUPLICATE-422 (Prisma `P2002` race-condition backstop maps to 422). Files shipped in `f94027d`: `src/lib/team-constants.ts` (EXTEND), `src/app/api/team/route.ts` (POST handler added alongside existing GET), `src/app/api/team/[id]/route.ts` (PATCH handler added alongside existing GET). No `src/lib/permissions.ts` change (matrix sufficient: TEAM_VIEW + TEAM_MANAGE). No schema, package, or config change. ActivityLog call sites added at canonical Section 23.6 Team taxonomy strings: `TEAM_MEMBER_ADD` (POST), `TEAM_MEMBER_UPDATE` (PATCH name change), `TEAM_MEMBER_ROLE_CHANGE` (PATCH role change); all remain deferred no-ops via `writeActivityLog()` until Step 4. Locked-by-default 401 contract preserved (`requireSession()` still null). PATCH uses corrected first-gate-then-mutation-auth pattern from D-2026-05-04-02: `requireAuth(TEAM_VIEW)` for entry, then `requirePermission(TEAM_MANAGE)` after target lookup. ChatGPT's same-day `/api/team` 404 from a brief check during deploy-swap was CDN edge propagation between `c94ae1e` (docs-only) and `f94027d` (3E-2A code); not a route defect. With 3E-2A done, only 3E-2B (team deactivate/reactivate) and 3F (modules) remain pending in Section 14 Step 3. See C-2026-05-05-05 for the implementation wave and C-2026-05-05-06 for this post-deployment doc-sync.
- **Step 4B-1 pushed, deployed, and Netlify-verified**: Supabase SSR packages and server helper live on `https://practice-iq.netlify.app/` since 2026-05-06 (commit `eb6dbc9`, full commit ref `eb6dbc94bfa8c11104e60334c91ff593f1bee428`, Netlify deploy `69fb36e36770d40008a61aed`, state `ready`, published 12:41:55 UTC, 46-second build, plugin success, 1 function deployed via `@netlify/plugin-nextjs@5.15.10` runtime `nodejs20.x`, scanned files count rose to 62 from 61 — confirms the runtime delta of one new file `src/lib/supabase-server.ts`). **Zero auth behaviour change verified end-to-end**: `GET /api/team`, `GET /api/team/dummy-id-for-401-check`, `GET /api/tasks`, and `GET /api/activity` all return 401 with the standard `{"ok":false,"message":"Authentication required."}` envelope; `GET /api/team/dummy-id-for-401-check/deactivate` and `GET /api/team/dummy-id-for-401-check/reactivate` return 405 Method Not Allowed proving POST-only route registration. `requireSession()` body in `src/lib/api-helpers.ts:49` continues to return null. `writeActivityLog()` continues to no-op. Locked-by-default 401 contract preserved. Files shipped in `eb6dbc9`: `package.json` (adds `@supabase/ssr` `^0.5.0` resolved to `0.5.2` and `@supabase/supabase-js` `^2.45.0` resolved to `2.105.3`), `package-lock.json` (regenerated by `npm install`), `src/lib/supabase-server.ts` (NEW, 85 lines; server-only helper exporting `createSupabaseServerClient()` that wraps `@supabase/ssr` `createServerClient` with `next/headers` `cookies()` using the canonical getAll/setAll pattern; lazy env reads inside the function body; throws on missing `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`; `SUPABASE_SERVICE_ROLE_KEY` strictly NOT used per Decision 4A-N1; explicit type annotation `cookiesToSet: { name: string; value: string; options: CookieOptions }[]` using the official `CookieOptions` type re-exported from `@supabase/ssr` to satisfy TypeScript strict mode). The helper is NOT imported by `src/lib/api-helpers.ts` or any route file — it is a dormant future dependency for Step 4B-2. `.env.example` unchanged. No schema change. No `next.config.ts` / `netlify.toml` change. No env-secret change. ChatGPT's same-day `/api/team` 404 from a brief check during deploy-swap was CDN edge propagation between `88aa14d` (docs-only) and `eb6dbc9` (4B-1 code); not a route defect (live state currently 401 / 405-as-expected). With 4B-1 deployed and verified, only Step 4B-2 / 4C / 4D / 4E / 4F / 4G remain pending under Step 4 per the locked sequence; Step 4B-2 is the next controlled implementation sub-wave and is the first wave to flip auth behaviour. See C-2026-05-06-03 for the implementation wave and C-2026-05-06-04 for this post-deployment doc-sync.
- **Step 4E-FU (firm-route audit call-site follow-on) pushed, deployed, and Netlify-verified**: `POST /api/firms` and `PATCH /api/firms/[firmId]` now emit `FIRM_CREATE` and `FIRM_UPDATE` ActivityLog rows respectively on successful authenticated mutations. Live on `https://practice-iq.netlify.app/` since 2026-05-06 (commit `662126d`, full commit ref `662126d03a9ee70d1d5271a087c1364169a3e1e8`, Netlify production deploy `69fb87f7e56dae000824ee16`, state `ready`, published 18:27:57 UTC, 52-second build, plugin success, 1 function deployed via `@netlify/plugin-nextjs@5.15.10` runtime `nodejs20.x` in region `us-east-2`, scanned files count 62 unchanged from `92a73f2`). **17-endpoint live regression matrix all green and identical to post-4E** (expected: audit emit is invisible to unauthenticated traffic; auth gate fires before mutation). `GET /api/team`, `GET /api/team/dummy-id-for-401-check`, `GET /api/tasks`, `GET /api/activity`, `GET /api/clients` → 401 envelope; `GET /api/team/dummy-id-for-401-check/deactivate` / `/reactivate` → 405; `POST /api/firms` (no auth) / `PATCH /api/firms/dummy` (no auth) → 401; `POST /api/firms/dummy/access` / `members` → 410; `POST /api/tenant/validate` (valid body) → public 200. **Audit payloads shipped**: (a) `POST /api/firms` → `writeActivityLog({ firmId: firm.id, actorId: session.userId, entityType: "FIRM", entityId: firm.id, action: "FIRM_CREATE" })` — `metadataJson` intentionally omitted because the new Firm record itself is the audit scope and full details are recoverable via DB join from `entityId`. (b) `PATCH /api/firms/[firmId]` → `writeActivityLog({ firmId: session.firmId, actorId: session.userId, entityType: "FIRM", entityId: updated.id, action: "FIRM_UPDATE", metadataJson: JSON.stringify({ fields: ["name","city","status","emailDomain"] }) })` — field names only, no values, matching the four columns the route writes. **Privacy guardrails**: no raw request body, no full firm snapshot, no headers, no cookies, no tokens, no passwords, no secrets, no env vars, no IP/user-agent, no firm name/city/emailDomain values. All identifiers are server-resolved cuids. **Auth/RBAC behaviour unchanged**: 4D gates remain — PLATFORM_OWNER inline check on POST; `requireAuth(Action.FIRM_UPDATE)` + cross-firm 404 + `console.warn` on PATCH; same 401 / 403 / 404 paths. **No new permission action** introduced; existing `Action.FIRM_UPDATE` from 4D unchanged. **No `src/lib/api-helpers.ts` change** — the 4E `writeActivityLog()` implementation is consumed unchanged. Files shipped in `662126d` (2 files; 32 insertions / 2 deletions per cached diff stat): `src/app/api/firms/route.ts` and `src/app/api/firms/[firmId]/route.ts` (each: one named import added; one audit emit block inserted after the Prisma write and before the success return). **No schema/migration/package/env/config/Netlify/Supabase change.** No `next.config.ts` / `netlify.toml` change. No `AGENTS.md` / `DECISION_LOG.md` change. No 4F PLATFORM_OWNER impersonation. No 3F or Step 5 work. **Externally observable behaviour at Stage 0 is unchanged** because the 200 / audit-write path requires an authenticated Supabase session (no users seeded; no login UI); the `/api/activity` GET read route already serves any rows that accumulate. With 4E-FU deployed and verified, the audit pipeline is comprehensive across **15 mutation actions** (13 from 4E + 2 firm-route actions added in 4E-FU). **Step 4E and its firm-route audit follow-on are now closed.** Only Step 4F (audited PLATFORM_OWNER cross-firm impersonation per Section 25.6) and 4G (Auth UAT and tenant leakage testing) remain pending under Step 4. See C-2026-05-06-07 (post-4E doc-sync), the 4E-FU implementation commit `662126d`, and C-2026-05-06-08 for this post-deployment doc-sync.
- **Step 4E pushed, deployed, and Netlify-verified**: Real `writeActivityLog()` live on `https://practice-iq.netlify.app/` since 2026-05-06 (commit `92a73f2`, full commit ref `92a73f2590885699ca94de4c2301abda3be04c96`, Netlify production deploy `69fb82120740f20008ad9cfe`, state `ready`, published 18:02:41 UTC, 44-second build, plugin success, 1 function deployed via `@netlify/plugin-nextjs@5.15.10` runtime `nodejs20.x` in region `us-east-2`, scanned files count 62 unchanged — confirms 4E is a single-file body change to `src/lib/api-helpers.ts`, not a new file). **17-endpoint live regression matrix all green and identical to post-4D** (expected: 4E is invisible to unauthenticated traffic because the auth gate fires before `writeActivityLog()` is reached). `GET /api/team`, `GET /api/team/dummy-id-for-401-check`, `GET /api/tasks`, `GET /api/activity`, `GET /api/clients` → 401 envelope; `GET /api/team/dummy-id-for-401-check/deactivate` / `/reactivate` → 405; `POST /api/firms` (no auth) / `PATCH /api/firms/dummy` (no auth) → 401; `POST /api/firms/dummy/access` / `members` → 410; `POST /api/tenant/validate` (valid body) → public 200. **Behaviour shipped**: `writeActivityLog()` body replaced from no-op (`return;`) with `prisma.activityLog.create({ data: { firmId, actorId, entityType, entityId ?? null, action, metadataJson ?? null } })`; missing `DATABASE_URL` returns early without throwing; the Prisma write is wrapped in a fail-open `try/catch`; on caught error a structured `console.error` records action, entityType, entityId, firmId, actorId, and error message; raw `metadataJson` is deliberately NOT logged (privacy guardrail for free-text fields like team deactivate/reactivate `reason`). Helper signature unchanged (`writeActivityLog(args: ActivityLogArgs): Promise<void>`); `ActivityLogArgs` shape unchanged. **No route file changed**: the 13 existing mutation call sites (2 clients in 3B; 7 tasks in 3D; 4 team in 3E) already pass non-null `firmId` and `actorId` from the post-4B-2 real `requireSession()`, so lighting up the helper alone is sufficient to start writing rows. `requireAuth()`, `requireSession()`, `parseJson()`, `ok()`, `err()`, `databaseUnavailable()` all unchanged. No new imports (`prisma` already imported since 4B-2). **Audit pipeline now live across 13 mutation actions**: `CLIENT_CREATE`, `CLIENT_UPDATE` / `CLIENT_SOFT_DELETE`, `TASK_CREATE`, `TASK_STATUS_CHANGE` / `TASK_REVIEWER_CHANGE` / `TASK_UPDATE`, `TASK_NOTE_ADD`, `TASK_ASSIGNEE_ADD` / `TASK_ASSIGNEE_REMOVE`, `TASK_CLOSE`, `TASK_REOPEN`, `TASK_CANCEL`, `TEAM_MEMBER_ADD`, `TEAM_MEMBER_UPDATE` / `TEAM_MEMBER_ROLE_CHANGE`, `TEAM_MEMBER_DEACTIVATE`, `TEAM_MEMBER_REACTIVATE`. **Externally observable behaviour at Stage 0 is unchanged** because the 200 / audit-write path requires an authenticated Supabase session (no users seeded; no login UI); the 401-by-default contract holds end-to-end. Files shipped in `92a73f2` (1 file; 57 insertions / 14 deletions per cached diff stat): `src/lib/api-helpers.ts` (writeActivityLog body + comment block rewrite). **No schema/migration/package/env/config/Netlify/Supabase change.** No `next.config.ts` / `netlify.toml` change. No `AGENTS.md` / `DECISION_LOG.md` change. **No firm-route audit call-site follow-on started** — `POST /api/firms` and `PATCH /api/firms/[firmId]` do not yet call `writeActivityLog()`; queued as a small follow-on micro-wave before Step 4F. No 4F PLATFORM_OWNER impersonation (Step 4F scope). No 3F or Step 5 work started. The 5 origin firms/tenant routes were hardened in 4D but their audit call sites were not added then or now (deferred from 4D plan-first; queued for 4E follow-on). With 4E deployed and verified, the audit pipeline is operational; the next controlled micro-wave is the firm-route audit call-site follow-on (~2 small route-file edits adding `writeActivityLog({ action: "FIRM_CREATE", ... })` and `writeActivityLog({ action: "FIRM_UPDATE", ... })`), and after that, **Step 4F** (audited PLATFORM_OWNER cross-firm impersonation flow per Section 25.6) is the next sub-wave proper. See C-2026-05-06-06 (post-4D doc-sync), the 4E implementation commit `92a73f2`, and C-2026-05-06-07 for this post-deployment doc-sync.
- **Step 4D pushed, deployed, and Netlify-verified**: Firms/tenant route hardening + F1/F2 clients cleanup live on `https://practice-iq.netlify.app/` since 2026-05-06 (commit `1b88f80`, full commit ref `1b88f803da7f599f956500fafc30f1e0d85d5123`, Netlify production deploy `69fb7d0071078d0008593e00`, state `ready`, published 17:41:04 UTC, 48-second build, plugin success, 1 function deployed via `@netlify/plugin-nextjs@5.15.10` runtime `nodejs20.x` in region `us-east-2`, scanned files count 62 unchanged — confirms 4D is body changes plus 410 stubs, not new routes). **Live regression matrix all 17 endpoints green** including the four new POST/PATCH gates: `POST /api/firms` (no auth) → 401 envelope; `PATCH /api/firms/dummy-id-for-401-check` (no auth) → 401 envelope; `POST /api/firms/dummy-id-for-401-check/access` → 410 Gone with "removed-endpoint" body; `POST /api/firms/dummy-id-for-401-check/members` → 410 Gone with "Use POST /api/team" body; `POST /api/tenant/validate` (valid `{"kind":"domain","payload":{"domain":"example.com"}}`) → public 200 `{"ok":true}`. All 14 already-protected routes regress 401 (or 405 on POST-only GET probes) unchanged. **Auth model shipped**: `POST /api/firms` is auth-gated to PLATFORM_OWNER only via inline `session.platformRole === PlatformRole.PLATFORM_OWNER` check (no new `Action.FIRM_CREATE`; the matrix gains nothing from a code that only PLATFORM_OWNER ever holds via its short-circuit per 4D-D1); `PATCH /api/firms/[firmId]` is auth-gated via new `Action.FIRM_UPDATE` (added to `src/lib/permissions.ts` and granted to FIRM_ADMIN base array; PLATFORM_OWNER short-circuit preserved per 4D-D2); URL `firmId` validated against server-resolved `session.firmId` with cross-firm hits returning 404 + `console.warn` per Section 25.4 #15 (no all-firm escape; PLATFORM_OWNER cross-firm access remains Step 4F impersonation scope). **Deprecations**: `POST /api/firms/[firmId]/access` and `POST /api/firms/[firmId]/members` now return 410 Gone via stub handlers; route files preserved so the URL space registers the deprecation cleanly rather than 404; `/api/firms/[firmId]/members` 410 body explicitly points to `POST /api/team` (canonical 3E-2A path). **`/api/tenant/validate` intentionally remains public** — pure stateless format validator, no DB read, no DB write, no PII, no firm-existence leak; the route file gained a documenting comment block explaining why and warning future audits not to add `requireAuth` without re-architecting the pre-login UI flow. **F1/F2 clients-route cleanup shipped**: `.strict()` added to `CreateClientSchema` and `UpdateClientSchema` (extra body fields now 422 instead of silent accept); cross-firm 404 branches in `GET /api/clients/[id]` and `PATCH /api/clients/[id]` split from real-not-found and now `console.warn` per Section 25.4 #15. Files shipped in `1b88f80` (8 files; 192 insertions / 141 deletions per cached diff stat): `src/lib/permissions.ts`, `src/app/api/firms/route.ts`, `src/app/api/firms/[firmId]/route.ts`, `src/app/api/firms/[firmId]/access/route.ts`, `src/app/api/firms/[firmId]/members/route.ts`, `src/app/api/tenant/validate/route.ts`, `src/app/api/clients/route.ts`, `src/app/api/clients/[id]/route.ts`. **No schema/migration/package/env/config/Netlify/Supabase change.** No `next.config.ts` / `netlify.toml` change. No `AGENTS.md` / `DECISION_LOG.md` change. No 4E activity logging (`writeActivityLog()` still no-ops; Step 4E scope). No 4F PLATFORM_OWNER impersonation (Step 4F scope). No 3F or Step 5 work started. Mandatory pre-coding grep on 2026-05-06 confirmed zero UI/source callers for the deprecated `/access` and `/members` routes; deprecation broke nothing. With 4D deployed and verified, only Step 4E (real `writeActivityLog()` writes), 4F (audited PLATFORM_OWNER cross-firm impersonation per Section 25.6), and 4G (Auth UAT and tenant leakage testing) remain pending under Step 4. **Step 4E is the next controlled implementation sub-wave** per the locked Step 4 sequence. See C-2026-05-06-05 (post-4B-2 doc-sync), the 4D implementation commit `1b88f80`, and C-2026-05-06-06 for this post-deployment doc-sync.
- **Step 4B-2 pushed, deployed, and Netlify-verified**: Real `requireSession()` live on `https://practice-iq.netlify.app/` since 2026-05-06 (commit `0c47cd7`, full commit ref `0c47cd74e2c231df15344d76ac6f9b73777d7cb0`, Netlify production deploy `69fb413880ff1f00084be0e4`, state `ready`, published 13:26:04 UTC, 50-second build, plugin success, 1 function deployed via `@netlify/plugin-nextjs@5.15.10` runtime `nodejs20.x` in region `us-east-2`, scanned files count 62 unchanged from `eb6dbc9` — `requireSession()` is a single-file body change, not a new file). **Live regression matrix all green**: `GET /api/team`, `GET /api/team/dummy-id-for-401-check`, `GET /api/tasks`, and `GET /api/activity` all return 401 with the standard `{"ok":false,"message":"Authentication required."}` envelope; `GET /api/team/dummy-id-for-401-check/deactivate` and `GET /api/team/dummy-id-for-401-check/reactivate` return 405 Method Not Allowed proving POST-only route registration. **Algorithm shipped** (folds 4C per Decision 4A-D1): (1) build server-side Supabase client via `createSupabaseServerClient()` with .catch fail-closed on env-config throw; (2) resolve user via `supabase.auth.getUser()` (validates JWT, never `getSession()`); (3) normalize email `trim().toLowerCase()` per Decision 4A-F1 + I1-NORMALIZE; (4) lookup `PlatformUser` by unique email; fail closed if absent or `isActive === false`; (5) narrow `platformRole` via `normalizePlatformRole()`; fail closed on corrupt value; (6) findMany active `FirmMember` rows; (7) Stage 0 fail-closed if `memberships.length !== 1` (applies uniformly to STANDARD AND PLATFORM_OWNER per the 2026-05-06 PLATFORM_OWNER edit approving option (b); zero memberships and multi-firm both fail closed); (8) narrow `firmRole` via `normalizeFirmRole()`; (9) build `SessionUser` with server-resolved `firmId` only — client-supplied `firmId` is never trusted (Decision 4A-G1). `SUPABASE_SERVICE_ROLE_KEY` not used (Decision 4A-N1). Files shipped in `0c47cd7`: `src/lib/api-helpers.ts` (single-file edit, 95 insertions / 10 deletions per cached diff stat; rewrites `requireSession()` body and adds three imports — `prisma`, `createSupabaseServerClient`, `normalizeFirmRole` + `normalizePlatformRole`). `requireAuth()` unchanged. `writeActivityLog()` unchanged (still no-op; Step 4E scope). No route file change. No schema/migration/package/env/config/Netlify/Supabase setting change. The 5 origin firms/tenant routes remain public — their hardening is Step 4D scope. PLATFORM_OWNER cross-firm impersonation remains Step 4F scope. **Externally observable behaviour at Stage 0 unchanged**: every external probe continues to return 401 because no Supabase users are seeded yet and no signup/signin UI is deployed; the 200 / 403 / 422 / 404 paths activate only once Supabase users exist mapped to active PlatformUser + exactly one active FirmMember. With 4B-2 deployed and verified, only Step 4D (route hardening + 5 origin routes), 4E (real `writeActivityLog()`), 4F (audited PLATFORM_OWNER impersonation), 4G (Auth UAT) remain pending under Step 4. See C-2026-05-06-04 (4B-1 doc-sync), the 4B-2 implementation commit `0c47cd7`, and C-2026-05-06-05 for this post-deployment doc-sync.
- **Step 4A architecture confirmed (deployed and verified; D-2026-05-06-02)**: Section 14 Step 4A architecture-confirmation wave deployed and Netlify-verified at commit `88aa14d` on 2026-05-06 (deploy ID `69fad62a03059300088f5b4b`, state `ready`, plugin success, 44-second build). 15 decisions locked (4A-A1 through 4A-O1) covering identity provider (Supabase Auth), session approach (server-managed cookie-based via `@supabase/ssr`; secure cookie settings verified during implementation, NOT over-locked as "HTTP-only" until 4B confirms behaviour), server integration (`@supabase/ssr` + `@supabase/supabase-js`; `@supabase/ssr` is in beta — implementation must follow current docs), session resolution location (`requireSession()` in `api-helpers.ts`), route protection (in-route `requireAuth`; no middleware RBAC), user mapping (by normalized email; no schema change), firm context (active FirmMember server-side lookup; never trust client firmId), PLATFORM_OWNER (no all-firm by default; impersonation in 4F), inactive-user behaviour (cascade to 401), multi-firm membership (deferred to Stage 1; Stage 0 fails closed on multi-active), origin route hardening (all 5 in 4D), ActivityLog real writes (4E, after 4D, before 4F), package isolation (4B split into 4B-1 prep + 4B-2 behaviour change), service-role key handling (server-only; NOT for ordinary session resolution), rollback model (per-sub-wave reverts; locked-by-default fallback intact). No code changed. No package/env changed. No schema changed. Supabase Auth + `@supabase/ssr` chosen, but implementation deferred to Step 4B-1 + 4B-2. Service-role key remains server-only and is NOT used for ordinary session resolution. 4B is split into 4B-1 (package/env/helper prep) and 4B-2 (real `requireSession()`) to reduce rollback risk and isolate the package-add blast radius from the auth-behaviour-change blast radius. See C-2026-05-06-02 and D-2026-05-06-02. Step 4 implementation has NOT started.
- **Section 14 reorder APPROVED — Step 4 before 3F (D-2026-05-06-01)**: the previously parked option from D-2026-05-05-03 is now formally approved. Revisit trigger fired on 2026-05-06: 3E is fully closed (3E-1 + 3E-2A + 3E-2B all deployed and Netlify-verified at runtime SHA `c5535f3`); the offline workpack on 2026-05-06 surfaced the critical finding that the 5 origin firms/tenant routes (`/api/firms/`, `/api/firms/[firmId]/`, `/api/firms/[firmId]/access/`, `/api/firms/[firmId]/members/`, `/api/tenant/validate/`) are NOT protected by `requireAuth(...)` today and must be hardened in Step 4 per D-2026-04-30-15 Decision 5. New locked sequence: 3D ✓ → 3E ✓ → **Step 4 Auth/RBAC** → **3F Modules** → Step 5 Persistence/UI cutover. 3F is NOT cancelled; it is repositioned. Step 4 implementation has NOT started; only the reorder approval is committed in this wave. No code changed in this decision wave. See D-2026-05-06-01 and C-2026-05-06-01.
- **Step 3E-2B pushed, deployed, and Netlify-verified**: Team deactivate/reactivate routes live on `https://practice-iq.netlify.app` since 2026-05-05 (commit `c5535f3`, Netlify deploy `69fa321b48b7bc0008dd7848`, full commit ref `c5535f3a46d419376ba3e3bb57fbc65543fff9c8`, state `ready`, published 18:09:18 UTC, 50-second build, plugin success, 24 new files uploaded which confirms runtime delta vs the earlier docs-only `21b7c72` deploy, scanned files count rose to 61 from 59). `GET /api/team` and `GET /api/team/dummy-id-for-401-check` return 401 locked-by-default. `GET /api/team/dummy-id-for-401-check/deactivate` and `GET /api/team/dummy-id-for-401-check/reactivate` return 405 Method Not Allowed (proves POST-only route registration; the genuine POST path hits `requireAuth(Action.TEAM_VIEW)` and would return 401 because `requireSession()` is null). `GET /api/tasks` and `GET /api/activity` regression checks remain 401. Implements approved decisions F1 deactivate variant (self-deactivation rejected with 422; `"Cannot deactivate yourself."`), G1 deactivate variant (last-active-FIRM_ADMIN deactivation rejected with 422; `"Cannot deactivate the last active firm admin."` — count query fires only when target is currently active FIRM_ADMIN), H1 (action strings `TEAM_MEMBER_DEACTIVATE` and `TEAM_MEMBER_REACTIVATE` per Section 23.4 + Section 23.6 pattern), 3E-2-M1 (deactivate/reactivate touches `FirmMember.isActive` only; NEVER touches `PlatformUser.isActive` — firm-scoped lifecycle, allows the user to remain active in other firms in Stage 1+), 3E-2-N1 (already-inactive deactivate returns 422; already-active reactivate returns 422 — explicit no-op detection mirroring the cancel-already-cancelled pattern from 3D-3), 3E-2B-P (post-ChatGPT review correction: `reason` field is REQUIRED on both deactivate and reactivate; trimmed string, min 1, max `MAX_TEAM_NOTE_LENGTH = 4000`). Files shipped in `c5535f3`: `src/lib/team-constants.ts` (EXTEND — adds `MAX_TEAM_NOTE_LENGTH = 4000`), `src/app/api/team/[id]/deactivate/route.ts` (NEW, POST only), `src/app/api/team/[id]/reactivate/route.ts` (NEW, POST only). No `src/lib/permissions.ts` change. No schema, package, or config change. No task reassignment per Section 23.4 (existing assignments survive a deactivated FirmMember). No deletion / hard delete. Both routes use the corrected first-gate-then-mutation-auth pattern from D-2026-05-04-02. Cross-firm hits return 404 + `console.warn` per Section 25.4 #15. ActivityLog metadata for both events: `{ reason }` (always populated; no null reason). Locked-by-default 401 contract preserved (`requireSession()` still null). ChatGPT's same-day `/api/team` 404 from a brief check during deploy-swap was CDN edge propagation between `21b7c72` (docs-only) and `c5535f3` (3E-2B code); not a route defect. With 3E-2B done, the full Section 14 Step 3E (Team route group: read + add/update + deactivate/reactivate) is complete. Backend route surface for Team is feature-complete; UI integration (Step 5) and Auth wiring (Step 4) remain. Only 3F (modules) remains pending in Section 14 Step 3 under the locked sequence (subject to parked reorder per D-2026-05-05-03; reorder NOT approved). See C-2026-05-05-07 for the implementation wave and C-2026-05-05-08 for this post-deployment doc-sync.

## Current Stage

**Phase 1 - Prototype hardening.** Section 14 status (post-cloud-Codex divergence reconciliation, D-2026-04-30-15):

- Step 1 (Foundation cutover): **DONE**. `next.config.ts` flipped off static export, `netlify.toml` updated, `.env.example` documents the env-var contract, four memory files at app root.
- Step 2 (Postgres + Prisma wiring): **PARTIALLY DONE**. Origin shipped Postgres provider + first migration + `src/lib/prisma.ts`. Pending: `AllowedFirmDomain` (origin used `Firm.emailDomain` single-string instead per D-2026-04-30-15), `UserNotificationPreference`, `NotificationLog`, `NotificationChannel` and `NotificationType` enums.
- Step 3 (API layer scaffold): **PARTIALLY DONE**. Sub-steps 3A (commit `093a816`), 3B (commit `d1fad2f`), 3C (commit `7e62c99`), 3D-1 (Tasks foundation + read/create routes, C-2026-05-04-01, commit `8754760`), 3D-2 (Tasks mutations: PATCH + notes + assignees, C-2026-05-04-03 / D-2026-05-04-02, commit `13d8b4f`), 3D-3 (Tasks lifecycle actions: close + reopen + cancel, C-2026-05-04-05 / D-2026-05-04-03, commit `8bcf4d1`), 3E-1 (Team foundation + read routes, C-2026-05-05-02 / D-2026-05-05-02, commit `caafcd2`), 3E-2A (Team add/update routes: POST `/api/team` + PATCH `/api/team/[id]`, C-2026-05-05-05 / D-2026-05-05-05, commit `f94027d`), and 3E-2B (Team deactivate/reactivate routes: POST `/api/team/[id]/deactivate` + POST `/api/team/[id]/reactivate`, C-2026-05-05-07 / D-2026-05-05-06, commit `c5535f3`) **DONE** — pushed, deployed, and Netlify-verified. Full Section 14 Step 3D (Tasks route group) and full Step 3E (Team route group: 3E-1 read + 3E-2A add/update + 3E-2B deactivate/reactivate) are now complete; the Team backend route surface is feature-complete from a route-surface perspective. Origin's 5 firm / tenant routes still present, **NOT auth-gated today** (must be hardened in Step 4 per D-2026-04-30-15 Decision 5). Pending: **3F Modules now DEFERRED until after Step 4 Auth/RBAC** per the formally approved reorder at D-2026-05-06-01.
- Step 4 (Supabase Auth + tenant-guard + RBAC): **PARTIALLY DONE; Step 4A architecture confirmed (deployed and verified) per D-2026-05-06-02; Step 4B-1 pushed, deployed, and Netlify-verified at commit `eb6dbc9` (deploy `69fb36e36770d40008a61aed`) per C-2026-05-06-04; Step 4B-2 pushed, deployed, and Netlify-verified at commit `0c47cd7` (deploy `69fb413880ff1f00084be0e4`) per C-2026-05-06-05; 4C role + firm-context resolution folded into 4B-2 per Decision 4A-D1; Step 4D pushed, deployed, and Netlify-verified at commit `1b88f80` (deploy `69fb7d0071078d0008593e00`) per C-2026-05-06-06 — firms/tenant route hardening and F1/F2 deferred clients-route cleanup both shipped in 4D; Step 4E pushed, deployed, and Netlify-verified at commit `92a73f2` (deploy `69fb82120740f20008ad9cfe`) per C-2026-05-06-07 — real fail-open `writeActivityLog()` shipped, 13 mutation call sites now write `ActivityLog` rows on success; Step 4E-FU firm-route audit call-site follow-on pushed, deployed, and Netlify-verified at commit `662126d` (deploy `69fb87f7e56dae000824ee16`) per C-2026-05-06-08 — `POST /api/firms` and `PATCH /api/firms/[firmId]` now emit `FIRM_CREATE` and `FIRM_UPDATE` ActivityLog rows; Step 4E and its firm-route audit follow-on are now closed; audit pipeline now covers 15 mutation actions; Step 4F is the next controlled implementation sub-wave**. Origin shipped `src/lib/tenant-guard.ts` (domain-validation utility only — NOT a session resolver). Step 3A added the codified permission matrix at `src/lib/permissions.ts` (Section 10); 3C extended it with `Action.ACTIVITY_VIEW`; pre-3E permissions touchup at `96c57db` added `Action.TEAM_VIEW` to FIRM_ADMIN base array. Step 4A locked the architecture (15 decisions; D-2026-05-06-02): Supabase Auth + `@supabase/ssr` server-managed cookie session (HTTP-only specifics verified during implementation, not over-locked); in-route `requireAuth(...)`; `requireSession()` resolution in `api-helpers.ts`; Supabase user → PlatformUser by normalized email; firm context via active FirmMember lookup; PLATFORM_OWNER no firm context by default; inactive cascades to 401; multi-firm deferred to Stage 1; service-role key strictly server-only and NOT used for ordinary session resolution. Step 4 implementation has NOT started; only the architecture confirmation is committed. Pending implementation sub-waves (per D-2026-05-06-02): 4B-1 (package/env/helper preparation — `@supabase/ssr` + `@supabase/supabase-js` install; NEW `src/lib/supabase-server.ts`; NO auth behaviour change), 4B-2 (real `requireSession()` implementation), 4C (role and firm context resolution; may fold into 4B-2), 4D (route-level regression + 5 origin firms/tenant routes hardening + F1/F2 cleanup), 4E (real `writeActivityLog()` writes), 4F (audited PLATFORM_OWNER impersonation flow), 4G (Auth UAT and tenant leakage testing).
- Step 5 (Persistence cutover): **NOT STARTED**. UI still uses localStorage; API routes exist but UI does not consume them yet.

## What Is Working

- Live URL serves PracticeIQ branding consistently.
- Login flow with role-aware access (Platform Owner + firm-domain users).
- Role-based dashboards (Platform Owner / Firm Admin / Partner / others) and section visibility.
- Task lifecycle Open → In Progress → Pending Client / Internal → Under Review → Closed.
- Multi-assignee, mandatory reviewer, reviewer-closes-not-assignee enforcement at UI level.
- Progress notes with author and timestamp, closure remarks.
- Assignment-level and client-level rollups.
- Project Review view with sequencing / reassignment controls.
- Team access management (add user, role update, deactivate / reactivate, password reset).
- Admin module controls and activity monitor.
- **Firm Setup section** (active firm profile + add additional firms, Platform Owner gated).
- localStorage persistence for the UI workspace state.
- Netlify Next.js Runtime active; `.next/` published; `@netlify/plugin-nextjs` auto-detected; security headers preserved.
- Five Netlify env vars configured.
- Supabase project provisioned in Mumbai with first migration applied.
- `npm run lint`, `npm run db:validate`, `npm run uat:check` all passing.
- **Section 10 permission matrix codified** at `src/lib/permissions.ts` (PlatformRole / FirmRole / Action constants including `ACTIVITY_VIEW`, `hasPermission`, `requirePermission`, UI label maps, role normalizers).
- **API helper foundation** at `src/lib/api-helpers.ts` (response envelopes, locked-by-default `requireAuth`, Zod-backed `parseJson`, deferred no-op `writeActivityLog`).
- **Clients API** at `/api/clients` (GET list paginated + POST create) and `/api/clients/[id]` (GET one + PATCH update with soft-delete via `status: "INACTIVE"`). Tenant-isolated by `firmId`; cross-firm hits return 404; auth-gated and locked-by-default until Step 4.
- **Activity API** at `/api/activity` (GET list paginated + filtered by `entityType` / `action` / `from` / `to`). Tenant-isolated by `firmId`; ARTICLE_STAFF server-scoped to own actor; PLATFORM_OWNER without firm context returns 400; auth-gated and locked-by-default until Step 4. Returns empty results until Step 4 lights up `writeActivityLog()`.

## What Is Partially Built

- API layer - 5 firm / tenant routes exist (origin); clients route group landed in 3B; activity read route landed in 3C; tasks/ route group landed in 3D-1, 3D-2, and 3D-3 (full Tasks surface complete); team/ read routes landed in 3E-1; team/ mutation routes pending as 3E-2A (add/update) and 3E-2B (deactivate/reactivate); modules/ route group pending as 3F.
- Auth - `tenant-guard.ts` (email / domain validation) and `permissions.ts` (Section 10 matrix) exist; Supabase Auth not yet replacing hardcoded password digest; `requireSession()` is a deliberate stub returning null until Step 4.
- RBAC - role-based UI visibility implemented; permission matrix codified at API layer (3A) and consumed by clients (3B) and activity (3C) routes; origin's 5 routes not yet on the matrix (deferred to Step 4 per Decision 5).
- Email reminder structure - referenced in UI, not sending.
- Notification preferences / log architecture - entities and enums not yet in schema (D-2026-04-30-10 work pending).
- Audit trail - `ActivityLog` table exists; read route is live (3C) but `writeActivityLog()` is still a documented no-op until Step 4 supplies a real `actorId` (D-2026-04-30-15 Decision 4); reads will return empty results until then.

## What Is Missing

- `team/` mutation routes - 3E-2A (POST `/api/team`, PATCH `/api/team/[id]`) and 3E-2B (POST `/api/team/[id]/deactivate`, POST `/api/team/[id]/reactivate`).
- `modules/` route group - 3F (subject to the parked reorder question per D-2026-05-05-03).
- Supabase Auth replacing hardcoded SHA-256 password digest.
- Hardening of origin's existing 5 routes (deferred to Step 4 per D-2026-04-30-15 Decision 5).
- `AllowedFirmDomain` table (origin used single `Firm.emailDomain` instead; revisit when multi-domain support is needed).
- `UserNotificationPreference`, `NotificationLog` tables and `NotificationChannel` / `NotificationType` enums.
- `ActivityLog` writes (no-op stub in place; lights up in Step 4).
- Email reminders sending (architecture only, no scheduler / template pipeline).
- Test suite (no smoke tests, no integration tests).
- Backup / monitoring beyond Supabase defaults.

## Known Technical Risks

(See MASTER_PROJECT.md Section 16 for full text.)

1. Static-export trap - **RESOLVED 2026-04-30** (Section 14 Step 1 close).
2. Single-file `src/app/page.tsx` carries large amount of UI, state, and logic. Modular split planned (`NEXT_TASKS.md` item 4).
3. localStorage is the prototype's only persistence layer for UI workspace state; data is browser-local until Section 14 Step 5.
4. Origin's `Firm.emailDomain` (single-string) instead of D-2026-04-30-10's planned `AllowedFirmDomain` table - acceptable for current single-firm prototype; revisit when commercial activation requires multi-domain firms.
5. Hardcoded Platform Owner SHA-256 password digest still ships in the client bundle. Removed in Section 14 Step 4 when Supabase Auth lands.
6. Compliance posture open (DPDP Act applicability, audit retention period, RLS configuration). Tracked under the Pilot-to-SaaS Scaling Guardrails framework (MASTER_PROJECT.md Section 22, pre-real-client-data checklist 22.5).
7. New 3B and 3C routes are correctly locked-by-default (401 until Step 4) - this is the safety contract, not a defect. Confirm Step 4 lights them up before any UI consumes them.

## Deployment Readiness

- Single-tenant prototype build: live, stable, Postgres-backed (via origin's first migration), serving from dynamic Next.js Runtime.
- Multi-firm GA: not ready. Blocked on full Step 4 (Auth + RBAC) and Step 5 (Persistence cutover) plus the Product Experience Review (Section 20.7).
- Production data safety: improved over earlier (Postgres exists), but hardcoded admin creds, RLS not configured, no audit log writes yet. Do not put real client confidential data into the platform yet.

## Next 5 to 10 Priority Tasks

In execution order, gated by Plan → Approval → Execution → Test → Log:

1. **Step 4F - PLATFORM_OWNER / firm context / impersonation flow** - design and implement audited PLATFORM_OWNER cross-firm impersonation flow per Section 25.6. New `requireAuthCrossFirm(action, targetFirmId)` helper writes `CROSS_FIRM_IMPERSONATE` ActivityLog row BEFORE granting cross-firm access. THIS IS THE 3F-CRITICAL PATH. Plan-first turn required before any code change. This is the next controlled implementation sub-wave per the locked Step 4 sequence.
2. **Step 4G - Auth UAT and tenant leakage testing** - full UAT pass with the route regression matrix and tenant leakage test plan; document any findings.
3. **Section 14 Step 3F - Modules routes** (after Step 4 closes, per D-2026-05-06-01) - flag toggling per firm; PLATFORM_OWNER only via the audited impersonation flow that 4F establishes. 3F plan to be re-presented as a fresh planning turn after Step 4G closes.
4. **Section 14 Step 5 - Persistence cutover** (after Step 4 AND 3F both close) - migrate UI from localStorage to API; one-time browser-side export endpoint; activate `ActivityLog` writes from the UI side.
5. **Decide on `Firm.emailDomain` vs `AllowedFirmDomain`** - confirm origin's single-string approach is acceptable or schedule the multi-domain extension (D-2026-04-30-15).
6. **Decide on `release-data-guard.mjs` removal** - origin deleted; do we reinstate (with our regex + ignored-files setup) or accept origin's removal?
7. **Continue Section 14 Step 2** - add `UserNotificationPreference`, `NotificationLog` entities and `NotificationChannel` / `NotificationType` enums to the schema (D-2026-04-30-10), or defer to Phase 2.
8. **Split `src/app/page.tsx` into modules** (`NEXT_TASKS.md` item 4; regression-risk reduction before broader UI changes).

## Validation Checklist (operational, before commit)

- Login page has no visible owner credential hint text.
- Branding shows `PracticeIQ` consistently in tab title, header, sidebar.
- Role-specific sections are hidden / shown correctly per profile.
- Firm Setup tab visible only to Platform Owner.
- `npm run uat:check` passes (lint + db:validate + build).
