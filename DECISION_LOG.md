# DECISION_LOG.md - PracticeIQ

Update rule: one entry per meaningful product, architecture, database, auth, deployment, or pricing decision.

Historical Phase-0 product decisions (pre-takeover) live in `00_Project_Memory/TOS_MEMORY.md` and `01_Product_Docs/13_DECISION_LOG.md`. This log starts at the takeover handoff on 2026-04-30.

---

## D-2026-04-30-01 - Database stack: Prisma + Supabase Postgres

- **Decision**: Persistence layer is Prisma ORM against Supabase Postgres.
- **Why**: Keeps the existing Prisma schema work, gives type-safe queries, versioned migrations, and matches what `DEPLOYMENT.md` already documents.
- **Alternatives rejected**: Supabase-only (drops Prisma; loses ORM type-safety; throws away schema work). Stay on SQLite (blocks any client-facing rollout beyond TAMS-TKG).
- **Impact**: Prisma `provider` flips from `sqlite` to `postgresql` in Step 2. `supabase/schema.sql` becomes a reference doc, not the live schema.

## D-2026-04-30-02 - Authentication: Supabase Auth

- **Decision**: Identity and sessions are managed by Supabase Auth (email + password to start).
- **Why**: Sessions, password reset, email verification, JWT issuance handled for us. Matches the chosen DB stack.
- **Alternatives rejected**: Custom JWT + bcrypt over Prisma (we own all security patches forever). NextAuth / Auth.js (adds a second auth abstraction on top of Supabase, fragments the session story).
- **Impact**: Hardcoded SHA-256 password digest in client bundle is removed in Step 4. `tenant-guard.ts` consumes Supabase session.

## D-2026-04-30-03 - Backend build order: Foundation first

- **Decision**: Sequence locked as Foundation → Postgres → API → Auth + RBAC → Persistence cutover.
- **Why**: Each step builds on the last with no rework. Avoids writing API routes without identity (which would force RBAC retrofit across every route).
- **Alternatives rejected**: Persistence first then auth (double-work on every route). Auth first then everything else (cleanest identity but app gates nothing useful until rest follows).
- **Impact**: Defined as MASTER_PROJECT.md Section 14, locked sequence. No reorder without explicit approval.

## D-2026-04-30-04 - Canonical product name: PracticeIQ

- **Decision**: External brand is PracticeIQ. TOS is retained as internal codename.
- **Why**: Matches project instructions, sounds like a SaaS brand, allows live URL rename when ready.
- **Alternatives rejected**: Keep TOS everywhere (weaker brand, conflicts with project instructions). Defer the name (keeps tension in every future doc).
- **Impact**: All new docs use PracticeIQ. Codebase rename and live URL change deferred to Phase 2 cutover.

## D-2026-04-30-05 - Target market: Indian CA first, CPA-ready architecture

- **Decision**: Phase 1 to 3 sells only to Indian CA firms. Architecture stays multi-tenant and locale-flexible. CPA market unlocked in Phase 4+.
- **Why**: Matches every existing product doc (PAN, GSTIN, Indian roles). Doesn't cap long-term addressable market.
- **Alternatives rejected**: Indian only forever (caps market, contradicts project instructions). CA + CPA from day one (immediate locale, currency, tax-ID polymorphism work that is not yet scoped).
- **Impact**: No locale or currency work in Phase 1 to 3. Tax-ID polymorphism is a known schema lift in Phase 4.

## D-2026-04-30-06 - Pricing model: Hybrid per-firm base + per-user beyond N

- **Decision**: Per-firm flat base fee that includes N seats, per-user fee above the cap. Tiered as Free Trial → Starter → Professional → Enterprise.
- **Why**: Captures small-firm simplicity and large-firm scaling. Matches existing `Plan` / `FirmSubscription` schema.
- **Alternatives rejected**: Per-user / month tiered only (leaves money uncaptured for small firms with simple needs). Per-firm flat (caps revenue as firms scale).
- **Impact**: Plan tier rows in DB will carry both `priceMonthlyBase` and `priceMonthlyPerExtraUser` plus `includedSeats`. Schema lift deferred until Phase 4 (commercial activation).

## D-2026-04-30-07 - Long-term roadmap: 5 phases, milestone-level, no calendar dates

- **Decision**: MASTER_PROJECT.md carries Phase 1 to 5 milestones without dates. Calendar timing lives in CURRENT_STATUS.md.
- **Why**: Pre-revenue timing depends on validation. Calendar slippage in MASTER would mean constant edits.
- **Alternatives rejected**: 12-month dated quarterly roadmap (too brittle). Separate ROADMAP.md (splits source of truth).
- **Impact**: MASTER stays stable. Date discussions and slips happen in CURRENT_STATUS only.

## D-2026-04-30-08 - Project memory file location: `02_App/tos-app/`

- **Decision**: MASTER_PROJECT.md, CURRENT_STATUS.md, DECISION_LOG.md, CHANGE_LOG.md live at the app root next to `package.json` and `AGENTS.md`, not at the repo root or in `00_Project_Memory/`.
- **Why**: Co-locates project memory with code so any developer (human or agent) finds them without hunting. Project instructions explicitly read from app root.
- **Alternatives rejected**: Repo root (separates from code). `00_Project_Memory/` (legacy chronological log location, mixes Phase-0 history with active project memory).
- **Impact**: `00_Project_Memory/TOS_MEMORY.md` and `01_Product_Docs/13_DECISION_LOG.md` are kept as historical Phase-0 archive. New decisions go here.

## D-2026-04-30-09 - Live URL renamed: `tos-tams-tkg.netlify.app` to `practice-iq.netlify.app`

- **Decision**: The Netlify site was renamed by Pankaj directly from `tos-tams-tkg.netlify.app` to `practice-iq.netlify.app`. The new URL `https://practice-iq.netlify.app/` is now the canonical live URL.
- **Why**: Aligns the public URL with the new product brand (PracticeIQ, locked in D-2026-04-30-04). Removes the legacy product-codename "tos" from the user-facing surface. Closes the URL-rename concern that was tagged HIGH-RISK in the Phase-1 rebrand plan.
- **Alternatives rejected**: Custom domain such as `app.practiceiq.in` (deferred to Phase 4 commercial activation). Keep both URLs alive via redirect (Netlify free-tier rename does not preserve the old hostname; not configurable).
- **Impact**: Old URL `https://tos-tams-tkg.netlify.app/` returns 404 ("Site not found") - confirmed by Pankaj via browser test on 2026-04-30. Any pre-existing bookmarks to the old URL are dead. Doc references updated (see C-2026-04-30-03). User-comms about the URL change is NOT required (Pankaj confirmed 2026-04-30: no real users to inform). Section 14 cutover requirement (zero-downtime through Step 1) now applies to the new URL. Wave 4 of the rebrand plan is fully complete.

## D-2026-04-30-10 - PracticeIQ identity, allowed-domains, and notification architecture

- **Decision**: Lock the SaaS identity and notification model. Two-tier identity: Platform Owner uses a dedicated SaaS root account (`admin@practiceiq.app`), never a personal email. Firm users sign in with their own firm-domain emails. Firms carry one or more `AllowedFirmDomain` records. Personal emails are permitted only as recovery / backup. Notification preferences live at user-level and firm-level. Channels: EMAIL, WHATSAPP, IN_APP. Types: DUE_REMINDER, OVERDUE_ALERT, DAILY_SUMMARY, REVIEW_PENDING, ACTION_REQUIRED, CLOSURE_CONFIRMATION. Mobile number is an optional profile field; WhatsApp is a future module gated by consent and firm-level configuration.
- **Why**: Pre-takeover code wired the Platform Owner to a personal Gmail and gated all logins to a single firm domain. That works for a single-tenant prototype, not for multi-firm GA. Locking the identity model now ensures Section 14 Steps 2 (schema) and 4 (auth) build the right structure first time. Email becomes the primary notification channel; WhatsApp follows once Phase-5 messaging infrastructure is in. Notification audit trail (`NotificationLog`) is required from day one of email sending so every reminder is traceable.
- **Alternatives rejected**: (a) Keep personal Gmail as permanent Platform Owner identity - security and brand risk; personal email mixed with SaaS root is poor hygiene. (b) Single shared `@practiceiq.app` domain for all firm users - collapses firm boundaries; users would not recognize their own identity inside their firm's workspace. (c) Defer the identity decision until Section 14 Step 4 - forces a seed and login rebuild twice and risks Step 2 schema being misshapen.
- **Impact**: Three new entities planned for Phase 2/4 schema work: `AllowedFirmDomain` (firm allowed-domain rule), `UserNotificationPreference` (per-user channel and type opt-ins), `NotificationLog` (audit trail of every send). Two new enums planned: `NotificationChannel` (EMAIL, WHATSAPP, IN_APP) and `NotificationType` (DUE_REMINDER, OVERDUE_ALERT, DAILY_SUMMARY, REVIEW_PENDING, ACTION_REQUIRED, CLOSURE_CONFIRMATION). `MASTER_PROJECT.md` Section 12 (data model) and Section 13 (Auth & RBAC) updated. Seed data: Platform Owner is now `admin@practiceiq.app` named "Platform Admin"; firm users use the placeholder firm domain `@demo-ca-firm.com`; personal Gmail is recovery-only and not wired as a primary login. Hardcoded SHA-256 password digest preserved on the new Platform Owner email so single-tenant login keeps working through the prototype phase; full replacement happens in Section 14 Step 4 with Supabase Auth. Login validator updated to accept the new firm domain plus the Platform Owner email.

## D-2026-04-30-11 - Rebranding cleanup closure: Wave 2B folder rename and Wave 3 localStorage key both deferred indefinitely

- **Decision**: The PracticeIQ rebrand initiative is closed at the user-facing surface. Two internal-only sub-waves are intentionally deferred indefinitely: (a) Wave 2B - folder rename `02_App/tos-app/` to `02_App/practiceiq-app/`; (b) Wave 3 - localStorage key migration from `tos-tams-tkg-live-v3` to a PracticeIQ-flavored key.
- **Why**: Both items are developer-internal only. The folder name is never user-visible; the localStorage key only appears in browser DevTools (99% of users never open it). Pankaj's standing principle: "if it works and is not user-visible, leave it alone." The cost of rename (doc churn, Netlify base-directory blast radius, workflow-break risk per AGENTS.md G1) outweighs zero user-visible benefit.
- **Alternatives rejected**: (a) Force the folder rename now - high doc-churn for cosmetic gain. (b) Force the localStorage rename now - non-zero workflow-break risk for invisible change. (c) Schedule a future maintenance window - speculative; no functional driver to commit to a date.
- **Impact**: Closes the rebrand initiative. `REBRANDING_CLOSURE_REPORT.md` produced at app root capturing what changed, what was deferred, what legacy remains and why, remaining risks, and Section 14 readiness. Section 14 Step 1 is unblocked pending Pankaj's approval of the closure report. Phase-0 internal names (`tamsEmailDomain` constant, `isTamsEmail` function, folder `02_App/tos-app/`, storage key `tos-tams-tkg-live-v3`, file `prisma/dev.db`) all retained with documented reasons.

## D-2026-04-30-12 - Product Intelligence Strategy locked: principles and three Phase-4/5 capabilities

- **Decision**: Lock the PracticeIQ Product Intelligence Strategy. Five principles govern any intelligence feature, present or future: (1) **rules first, AI second** - templates and deterministic logic before any model call; (2) **contextual and lightweight** - intelligence appears in the moment of work, never as a separate "AI panel"; (3) **human approval mandatory** - the platform suggests, recommends, warns; the user approves and acts; auto-execution is never the default; (4) **confidential by design** - every AI call respects firm tenancy, allowed-domain boundaries, audit logging, and notification preferences; (5) **predictable cost** - module-flag gated and per-firm capped. Three specific capabilities recorded as Phase-4/5 roadmap items, NOT for current build: (a) AI-assisted assignment tree builder; (b) capacity-aware assignment recommendation; (c) smart daily execution briefing.
- **Why**: Pankaj's directive to make the platform intelligence-assisted without derailing the locked Section 14 backend foundation work. Locking the principles now means Phase-1-to-3 backend work anchors the right hooks (templates, audit log, notification preferences, module flags) so intelligence layers slot in cleanly later. Recording the three capabilities up front prevents them from accumulating as ad-hoc requests during Phase 2-3 work.
- **Alternatives rejected**: (a) Build any intelligence feature in Phase 1 - derails locked Section 14 sequence. (b) Defer the strategy until Phase 5 - risks anchoring Phase-1-to-3 hooks incorrectly and missing data-model implications. (c) Build the three capabilities as full AI-first features with no rules-first layer - higher cost, more maintenance, less predictable than the rules-first approach.
- **Impact**: New `MASTER_PROJECT.md` Section 19 "Product Intelligence Strategy" captures the principles and the three roadmap items. Section 17 Phase 5 line updated to reference Section 19. Section 14 backend strategy unchanged - locked sequence preserved per the user's standing rule. None of the three capabilities are built in this turn or anytime before Section 14 Steps 1 to 5 are complete. Future intelligence features must conform to the five principles or get an explicit exception decision recorded in DECISION_LOG.

## D-2026-04-30-13 - Product Experience & Guided UX Principles locked: eight principles + Design Review gate before beta

- **Decision**: Lock the PracticeIQ Product Experience and Guided UX Principles. Eight principles govern all UI work, present and future: (1) **visual design direction** - calm, professional, clean, business-grade for CA / CPA firm operations; not flashy, childish, overloaded, or generic-dashboard-template; (2) **role-based UX** - the platform experience changes by role (Platform Owner, Firm Admin, Partner, Manager, Article / Staff); each role sees only what is relevant; (3) **guided workflow design** - every screen shows what needs attention, what is pending, what action is expected, what should happen next; (4) **progressive input capture** - inputs captured step by step, lightweight prompts, MCQ-style interactions, no field-floods; (5) **interactive decision support** - the platform recommends, the user decides, human approval mandatory; (6) **low-clutter interface** - no unnecessary panels, floating widgets, or excessive AI suggestions; (7) **Design Review Gate** - mandatory Product Experience Review before beta release, covering visual quality, role-based usability, navigation, guided workflow, input burden, decision support, and new-user operability without full training; (8) **final UX objective** - "guided execution system, not a static task tracker."
- **Why**: Pankaj's directive to lock product experience principles before any UI redesign begins, so Phase 1 backend foundation anchors the right hooks (role-based surfacing, progressive capture state, decision-support hooks, audit log of approvals). Locking now prevents drift toward generic-dashboard-template aesthetic during Phase 2-3 implementation. Beta release is gated on the Product Experience Review.
- **Alternatives rejected**: (a) Defer UX principles until Phase 2 - risks Phase 1 backend and seed UX work shipping with anti-patterns that need rework. (b) Begin UI redesign now - derails the locked Section 14 backend foundation. (c) Make the Design Review optional - removes the only structural quality gate before beta and makes drift acceptable.
- **Impact**: New `MASTER_PROJECT.md` Section 20 "Product Experience & Guided UX Principles" captures the eight principles. Section 17 Phase 2 line updated to make Product Experience Review approval the entry gate to beta / GA. Section 14 backend strategy untouched - locked sequence preserved. No UI implementation work begins now; each principle becomes its own Plan → Approval → Execution → Test → Log cycle when Phase 2+ work reaches it. Phase-0 docs `01_Product_Docs/06_UI_UX_BLUEPRINT.md` and `01_Product_Docs/16_UI_UPGRADE_BLUEPRINT.md` remain historical archives; Section 20 supersedes them as the active reference for UI work going forward. Future UI features must conform to the eight principles or get an explicit exception decision recorded in DECISION_LOG.

## D-2026-04-30-14 - Product strategy and release-readiness principles locked

- **Decision**: Lock seven strategic principles in MASTER_PROJECT.md. (1) **Build C, show A reinforced** with the concrete list of currently-scaffolded-but-hidden modules: Client Portal, Document Upload, WhatsApp Reminders, AI Assignment Builder, Advanced Reports, Billing / Subscription, Email Integration. These surface only in a controlled Plan & Modules / Upgrade Features area, not as locked buttons on operational screens. (2) **Plan tier feature contents locked**: Starter = Tasks, Clients, Team, Basic reports; Professional = Starter + Recurring workflows + Client Request Reminder Engine + Advanced reports + Email reminders; Enterprise = Professional + Client Portal + Document Upload + WhatsApp + AI assignment builder + Capacity intelligence + Audit log search + Advanced admin controls. Pricing values remain TBC. (3) **Client Request Reminder Engine ships before any Client Portal** - solves client-delay problem without document-storage risk. (4) **Client Portal Security Pre-Conditions** - 11 specific items must each be approved and recorded in DECISION_LOG before document upload ships. (5) **Monetization Discipline** - four standing don'ts protecting Section 14 sequence and keeping operational UX clean. (6) **Three release-readiness gates** overlay the existing 5-phase roadmap (Pre-Beta inside Phase 1; Pre-Paid-Launch inside Phase 3; Post-Launch / Premium inside Phase 5). (7) **"Fast or clutter" acceptance test** added to Section 20.6.
- **Why**: Pankaj's directive to lock product strategy and release-readiness principles before Phase 2-3 work begins, so feature shipping order is governed by user value and security pre-conditions, not by ad-hoc commercial pressure. Reminder engine before Client Portal solves the operational problem (client-side delays slowing closure) without taking on document-storage risk. Locking 11 portal security pre-conditions removes any "ship now, secure later" path. Defining plan tier contents now lets the seed and admin UI scaffold the right gating from the start.
- **Alternatives rejected**: (a) Build Client Portal in Phase 2 to drive premium revenue earlier - violates security-first principle; storage and audit posture not ready. (b) Leave plan tier contents TBC until Phase 4 - means firm admins cannot see what they're paying for during onboarding; pushes the decision into commercial-pressure territory. (c) Skip the reminder engine and rely on out-of-band email - misses the pending-request audit trail, breaks the workflow loop. (d) Keep the existing 5-phase roadmap without overlaying release gates - leaves the entry conditions implicit and contestable under pressure.
- **Impact**: New `MASTER_PROJECT.md` Section 21 captures 21.1 (reminder engine), 21.2 (portal security pre-conditions), 21.3 (monetization discipline). Section 4 expanded with scaffolded-but-hidden module list. Section 5 expanded with per-tier feature contents table. Section 17 restructured with three release-readiness gates as bullets within the 5-phase plan (no phase removed or renamed). Section 20.6 gains the "fast or clutter" acceptance test. Section 14 untouched - locked sequence preserved. New module flags planned: `CLIENT_REQUEST_ENGINE`, `CLIENT_PORTAL`, `DOCUMENT_UPLOAD` (Phase 2/3 schema work; natural extensions of existing `ModuleFlag` table). None of these capabilities are built in this turn. Each becomes its own Plan → Approval → Execution → Test → Log cycle when its phase arrives.

## D-2026-04-30-15 - Cloud-Codex divergence reconciliation: adopt origin/main as code base; preserve local governance docs

- **Decision**: Local working tree was reset to `origin/main` (`eaac64f`) on 2026-04-30. The cloud Codex co-work environment had pushed 8 commits that materially advanced the backend (Postgres schema, first migration, 5 API routes, `tenant-guard.ts`, Prisma client singleton) ahead of our locked Section 14 plan. Live URL serves origin/main. Local memory documentation (`MASTER_PROJECT.md`, `DECISION_LOG.md`, `CHANGE_LOG.md`, `REBRANDING_CLOSURE_REPORT.md`, plus G1-G6 Working Rules in `AGENTS.md` Section 9) is preserved as the governance layer on top of origin's code base.
- **Why**: Origin is what ships and what users see. Our local code edits had not been pushed and had been overtaken by the cloud Codex environment's work. Force-pushing local would have wiped origin's deployed backend progress. The safest reconciliation is to adopt origin's code, layer our governance docs on top, and document the divergences for resolution in subsequent steps.
- **Alternatives rejected**: (a) Force-push local over origin - catastrophic; would have wiped deployed backend. (b) Pure file-by-file merge - too large, too fragile (32 files, +8752 / -7279 lines diff). (c) Discard our local docs and start from origin's `NEXT_TASKS.md` / `PROJECT_CONTEXT.md` alone - loses the architectural decisions, principles, and audit trail we built (Sections 19, 20, 21; D-01 through D-14; the rebrand closure record).
- **Architectural divergences logged for follow-up**:
  - (a) Origin uses `Firm.emailDomain` (single string) instead of D-2026-04-30-10's `AllowedFirmDomain` table (multi-domain per firm). Acceptable for current single-firm prototype; revisit when a firm needs more than one domain.
  - (b) `UserNotificationPreference`, `NotificationLog`, `NotificationChannel`, `NotificationType` are NOT in origin's schema. They remain Phase-2 work per D-2026-04-30-10.
  - (c) `scripts/release-data-guard.mjs` was deleted by origin. `package.json` no longer has `release:data-guard`. `release:check` was renamed to `uat:check`. Decision pending: reinstate (with our regex + ignored-files setup) or accept origin's removal.
  - (d) Origin's seed firm uses domain `practiceiq.in` (real-looking) instead of D-2026-04-30-10's planned `@demo-ca-firm.com` placeholder. Origin's Platform Owner is still `singhal.accuron@gmail.com` instead of D-2026-04-30-10's planned dedicated `admin@practiceiq.app`. Both kept as origin shipped them; revisit when commercial activation requires hardening.
- **Impact**: All Section 14 step statuses updated in `MASTER_PROJECT.md` v1.7 (Step 1 done; Steps 2, 3, 4 partially done; Step 5 not started). `CURRENT_STATUS.md` rewritten to merge our phase tracker with origin's repo-health facts. `AGENTS.md` G1-G6 working rules ported into origin's `AGENTS.md` as Section 9. `NEXT_TASKS.md` and `PROJECT_CONTEXT.md` from origin retained as supporting handover docs. `.env.example` re-augmented with `DIRECT_URL` and the comment block (lost during reset). `.gitignore` extended with a narrow Netlify rule. No Prisma schema, migration, API route, page.tsx, or `.env.local` edits in this reconciliation wave.

## D-2026-05-03-01 - Pilot-to-SaaS Scaling Guardrails adopted

- **Decision**: Adopt a lightweight, stage-gated governance framework for PracticeIQ. Three named stages (Stage 0 Founder-led POC; Stage 0.5 Controlled beta candidate; Stage 1+ Commercial SaaS) with explicit lists of what is acceptable and not acceptable at each stage, stage-gate trigger points for company-owned accounts / paid plans / backup / support / legal terms / data policies, plus pre-real-client-data and pre-paid-client checklists. Captured as `MASTER_PROJECT.md` Section 22 (eight subsections, ~95 lines). Platform Ownership Register population is deferred (Option A); the template is named in 22.7 but not populated.
- **Why**: PracticeIQ is currently a founder-led POC built by a non-technical founder using AI-assisted development. Without a named stage and explicit guardrails, drift toward real-client-data usage before Section 14 Steps 4 (Auth) and 5 (Persistence) are complete is a real risk. Equally, full SaaS-company formalisation (SOC2, DPA templates, insurance, company-owned accounts) at the POC stage would add overhead without value. The framework names the stage we are in, lists what is and is not allowed, and pre-commits which formalisation steps unlock at which commercial milestone.
- **Alternatives rejected**: (a) Full SOC2 / ISO27001 / DPA preparation now - premature; no clients, no revenue, overhead with no value at this stage. (b) No governance framework at all - real risk of accidental drift to real-client-data usage before Steps 4 + 5 complete. (c) Adopt only to a side file outside `main` - does not bind future agents because the Plan-First protocol reads memory files at task start; side-file governance is invisible to the controlled-builder loop. (d) Adopt and populate the Platform Ownership Register in the same wave - feasible but requires founder to dictate account emails, recovery methods, billing owner now; deferred so the framework lands without that friction.
- **Impact**: New `MASTER_PROJECT.md` Section 22 (eight subsections) captures stage definitions, acceptable / not-acceptable lists at Stage 0, stage gates and trigger points, both checklists, the Platform Ownership Register template pointer, and an explicit Section 14 non-impact statement. `CURRENT_STATUS.md` Repo Health gains a one-line "Stage gate" marker recording the current stage; Risk #6 updated to cross-reference Section 22. Section 14 backend execution sequence untouched. Stage 0.5 promotion blocked until Section 14 Steps 4 + 5 complete and the pre-real-client-data checklist passes; Stage 1+ promotion blocked until the pre-paid-client checklist passes. Future agents reading memory files at task start must check the Stage gate before recommending any action involving real client data.

## D-2026-05-03-02 - Pre-3D Product Architecture & SaaS Guardrail Scan adopted

- **Decision**: Adopt the Pre-3D Product Architecture & SaaS Guardrail Scan as `MASTER_PROJECT.md` Section 23. Lock the canonical `TaskStatus` set (`OPEN`, `IN_PROGRESS`, `PENDING_CLIENT`, `PENDING_INTERNAL`, `UNDER_REVIEW`, `CLOSED`, `CANCELLED` - `REOPENED` is NOT a status), the `Priority` set (`LOW`, `NORMAL`, `HIGH`, `CRITICAL`), the lifecycle transition matrix, reopen-as-action rules (`CLOSED → IN_PROGRESS` only via approved reopen action; `closedAt` and `closedById` cleared; reason captured as `TaskNote`; ActivityLog action `TASK_REOPEN`), cancel-as-terminal rules, inactive user and inactive client behaviour, cross-firm ID validation requirements, multi-assignee and reassignment rules, due-date rule (mandatory, valid ISO datetime, **past dates ALLOWED for backlog tasks**), progress note minimums, closure remarks rule, the canonical Task-entity audit event taxonomy, the canonical plan-tier feature / module code list (now including `WRITING_ASSIST` at Professional+), the `requireEntitlement(firmId, featureCode)` helper SHAPE only (not implementation), and the Writing Assist feature concept and guardrails as a deferred Phase 4/5 capability.
- **Why**: 3D Tasks routes are the next implementation step and Tasks are the core operating engine of PracticeIQ - they affect reporting, audit logs, permissions, reminders, paywall controls, future AI, and persistence cutover. Without locking the status / priority sets, transition matrix, reopen / cancel semantics, inactive-entity behaviour, audit taxonomy, and entitlement helper shape before 3D, decisions would scatter across route files and contradict later. Locking the plan-tier feature codes now makes Section 23.7 the single source of truth that future routes (recurring tasks, advanced reports, writing assist) consume. Locking the entitlement helper shape now (without implementation) so future routes import a stable signature when the first paywall-gated feature ships. Locking Writing Assist as a concept now ensures 3D does not introduce schema or field decisions that would block future forward-compatibility.
- **Alternatives rejected**: (a) No pre-3D scan - 3D plan would invent these decisions inline and likely contradict later 3E / 3F / Step 4 work. (b) Lock only the must-lock-before-3D items (Option A from the proposal) - would defer plan-code lock and entitlement shape until they're needed in anger; both are cheaper to lock now than during a paywall-route wave. (c) Adopt to a side file outside `main` - invisible to the Plan-First protocol and to AGENTS.md G7. (d) Add `REOPENED` as a persistent `TaskStatus` - corrected per Pankaj 2026-05-03; reopen is a one-shot action that returns the task to `IN_PROGRESS`, not a durable state to dwell in; keeping the status set lean and the action set rich. (e) Reject past due dates at POST - corrected per Pankaj 2026-05-03; firms enter backlog tasks at onboarding and during ad-hoc cleanup, so past due is allowed and surfaces as overdue in reports. (f) Build the `requireEntitlement` helper now alongside 3D - over-investment; the helper has zero call sites in 3D core CRUD and the shape lock is sufficient until a paywall-gated route ships.
- **Impact**: New `MASTER_PROJECT.md` Section 23 (ten subsections, ~175 lines) captures all locked content. `CURRENT_STATUS.md` Repo Health gains a one-line marker recording adoption and pointing 3D at Section 23. `AGENTS.md` Section 9 gains rule G7 requiring future entity route groups (3D Tasks, 3E Team, 3F Modules) to consume Section 23 (or its named successor) before route planning. Section 14 backend execution sequence untouched. No schema change introduced. No code or route file edits in this wave. The 3D plan turn (next) reads Section 23 as canonical implementation constraints - including the corrected reopen-as-action rule, the corrected lifecycle transition matrix, the past-due-allowed rule, and the deferred entitlement helper. The 3D wave will create one new constants file (`src/lib/task-constants.ts`) carrying `TaskStatus`, `Priority`, and `TASK_STATUS_TRANSITIONS`. Future audit trail (lit up in Step 4) uses the locked Task-entity action strings from 23.6 from day one. The first paywall-gated route (likely `RECURRING_TASKS` in Phase 2/3) implements the `requireEntitlement` helper using the shape from 23.8.

## D-2026-05-03-03 - Governance File Maintenance & Independent Review Protocol adopted

- **Decision**: Adopt the Governance File Maintenance & Independent Review Protocol as `MASTER_PROJECT.md` Section 24. Lock the Tier 1 control pack (`MASTER_PROJECT.md`, `CURRENT_STATUS.md`, `DECISION_LOG.md`, `CHANGE_LOG.md`, `AGENTS.md`), the Tier 2 on-demand review list, the canonical role of each Tier 1 file, the 8 mandatory synchronization rules, the 11-point pre-commit consistency check (with GREEN / YELLOW / RED reporting), and the independent ChatGPT review gate as a control input (not approval) at named milestones (before major Section 14 steps, after meaningful commits on `main`, before Stage 0.5 / Stage 1+ promotion, when changing MASTER / AGENTS / schema / auth / tenant isolation / paywall / audit / deployment). New `AGENTS.md` rule G8 codifies the pre-commit check and the ChatGPT-review-is-control-input principle. Standardised wording for the check trigger (used in both Section 24.5 and AGENTS G8): "The Tier 1 consistency check applies before every commit-grade wave, whether code or documentation. It does not apply to every minor chat response or exploratory discussion. It applies when Claude is preparing changes that may be staged, committed, pushed, or used as the basis for a major Section 14 step."
- **Why**: Recent independent review identified stale architecture / status wording across `MASTER_PROJECT.md` (Section 8 sqlite-when-Postgres, Section 14 Step 3 clients/activity-pending after shipped, Section 14 Step 4 permission matrix-pending after 3A landed, Section 16 Risk #4 sqlite) and `CURRENT_STATUS.md` (push-pending after the actual push) even though the project had progressed correctly. The drift was caught only by happenstance, not by structural control. The protocol turns these failure modes from "caught by independent review" into "blocked by check before commit". 3D will be the first execution wave run under the new protocol; locking it now means 3D's plan turn benefits from the consistency check from the start. The standardised wording specifically scopes the check to commit-grade waves so trivial chat exchanges are not over-formalised.
- **Alternatives rejected**: (a) No protocol - drift continues; we rely on luck and on independent review catching things after they ship. (b) Run the consistency check after commit instead of before - moves the catch back into the same failure mode we just fixed; the point of the check is to block bad commits, not to log them. (c) Full external audit (formal third-party review) - premature at Stage 0 POC; expensive without proportionate value. (d) Apply the check to every chat response - over-formalises trivial conversation and exploratory discussion; the standardised wording explicitly limits the trigger to commit-grade waves to avoid this failure mode. (e) Adopt only G8 without MASTER Section 24 - violates the protocol's own Synchronization Rule #1 (any new agent behaviour rule paired with the canonical "why" in MASTER); self-defeating.
- **Impact**: New `MASTER_PROJECT.md` Section 24 (seven subsections, ~110 lines) captures Tier 1 / Tier 2 file lists, canonical roles, 8 synchronization rules, the 11-check pre-commit consistency check with GREEN / YELLOW / RED reporting and the standardised trigger wording, ChatGPT review gate triggers and rules, and Section 14 non-impact. New `AGENTS.md` rule G8 in Section 9 codifies the pre-commit check trigger using the standardised wording and references MASTER 24.5 and 24.6. New `CURRENT_STATUS.md` bullet under Repo Health records that the protocol is active and points at Section 24 / G8. New `CHANGE_LOG.md` entry C-2026-05-03-03 records this documentation-only wave. Section 14 backend execution sequence untouched. No schema, code, or route change. The protocol applies from C-2026-05-03-03 (this commit) forward; the 3D plan turn (next) runs the pre-commit check in its execution wave.

## D-2026-05-03-04 - Cost Discipline at Stage 0 adopted

- **Decision**: Adopt cost discipline as a standing operating principle for the founder-led POC stage. Captured as `MASTER_PROJECT.md` Section 22.9 (under the Pilot-to-SaaS Scaling Guardrails framework) and as `AGENTS.md` rule G9 (binding on all future agent recommendations). Order of preference: existing stack → free / built-in tier → open-source self-hosted → paid tier with clear justification. Every paid recommendation must answer four questions in writing (why free is not enough; when paid becomes necessary; what trigger flips the decision; what the downgrade path is) and must fall into one of six acceptable reason categories (security; data safety; reliability; unavoidable API cost; client / revenue trigger; future scalability). Vendor prices and exact free-tier limits are NOT hardcoded in governance files; the rule is principle-based and the agent verifies current pricing from official sources at the time of recommendation. Named example vendors in 22.9 tables are illustrative only, not locked vendor decisions.
- **Why**: PracticeIQ is currently a founder-led POC built by a non-technical founder using AI-assisted development. Capital and time both flow at this stage. Without an explicit cost-discipline rule, recommendations drift toward enterprise-tier tooling that adds spend without proportionate value at Stage 0. The principle complements Section 22.4 stage gates (which specify when paid plans become acceptable for PracticeIQ's own infrastructure) by governing how every recommendation is framed in the meantime. AGENTS G9 makes it binding so future agents read it at task start and apply it without being reminded.
- **Alternatives rejected**: (a) No formal rule - recommendations drift; the founder has to push back on every paid suggestion. (b) Hardcode vendor prices and free-tier limits in MASTER - prices and limits change frequently; would create stale governance and trigger documentation churn for no proportionate value. (c) Bundle into Section 25 Security wave - conflates two distinct concerns; cost discipline is a Stage 0 operating rule, not a security control. (d) Standalone new MASTER Section 26 - adds doc surface area when Section 22 already houses the Stage 0 framework that cost discipline naturally extends.
- **Impact**: New `MASTER_PROJECT.md` Section 22.9 (~80 lines) captures the order of preference, the four-question template for paid recommendations, the six acceptable-reason categories, the existing-stack-default table (8 categories, references "the five Tier 1 governance file pack"), the paid-acceptable-when-triggered table (6 categories with named example vendors), and the cross-references to Sections 22.4 / 23.7 / 24.4. `MASTER_PROJECT.md` Section 0 metadata refreshed: Document version v2.0 → v2.1; Last-meaningful-update line refreshed to "2026-05-03 (post Cost Discipline at Stage 0, D-2026-05-03-04)". New `AGENTS.md` rule G9 (~14 lines) makes the four-row recommendation template binding on every future proposal. New `CURRENT_STATUS.md` Repo Health bullet records that cost discipline is active; CURRENT_STATUS Last-updated header refreshed to also cite C-2026-05-03-04. New `CHANGE_LOG.md` entry C-2026-05-03-04. Section 14 backend execution sequence untouched. No retrospective re-justification of already-approved tooling. The rule applies forward from this commit; the pending Section 25 Security proposal (still under hold) gets re-presented under the new G9 four-row template before its commit.

## D-2026-05-03-05 - External Threat Security & Platform Hardening Guardrails adopted

- **Decision**: Adopt the External Threat Security & Platform Hardening Guardrails as `MASTER_PROJECT.md` Section 25. Locks: current security posture summary (25.1); external attacker model (25.2); OWASP-style risk coverage (25.3); permanent route-construction security checklist (25.4, 15 rules); task-route-specific security requirements for 3D (25.5, including length caps, Zod `.strict()`, ActivityLog metadataJson policy, no-HTML rendering); Step 4 security requirements (25.6); database and RLS requirements (25.7, RLS required before real client data); platform / config hardening (25.8, including CSP / HSTS / Permissions-Policy before friendly pilot); monitoring and incident response (25.9, including incident log + restore drill + severity classification); future module security (25.10, references Sections 21.2 and 23.9); G9 cost-discipline matrix (25.11, ten spend-touching items with four-row treatment, vendor-neutral, named examples only); CA / CPA Client Trust & Failure Scenario Risk Lens (25.12, twelve client concerns + ten failure scenarios + plan comparison); Platform Ownership Register status clarification (25.13, deferred during Stage 0 + mandatory before friendly pilot, not a contradiction of Section 22.7); and consumption rule (25.14, Section 25 must be consumed by future API and entity route planning alongside Section 23, AGENTS G7's effective scope is hereby extended to include Section 25, no new AGENTS G10 is required, Section 14 non-impact). Decisions G / H / I from the on-hold 3D plan (input length caps; Zod `.strict()`; ActivityLog metadataJson policy) become locked Section 25 guardrails consumed at 3D-1 implementation.
- **Why**: 3D Tasks routes will materially increase the API attack surface because tasks involve clients, assignees, reviewers, notes, status movement, close / reopen / cancel actions, and tenant-bound records. Locking the security posture before 3D ensures the route plan turn consumes a single canonical security reference rather than inventing or skipping controls inline. The CA / CPA client-trust lens reframes the same controls from the customer perspective, surfacing operational gaps (restore drill, Platform Ownership Register population, doc-export endpoint, recovery documentation) that pure technical-security framing misses. The G9 cost-discipline matrix proves that comprehensive security at Stage 0 / 0.5 requires zero new spend, with the first paid item (transactional email for password reset notifications) firing only at friendly pilot (per 25.11.8). G7's effective scope is extended via this DECISION_LOG entry rather than via a new AGENTS G10, because G7 already requires canonical-section consumption and a new G10 would create overlap and ambiguity.
- **Alternatives rejected**: (a) No security governance section - 3D plan would invent decisions inline; later route groups would not have a canonical reference; client-trust questions would surface only at friendly pilot when too late to redesign. (b) Add new AGENTS G10 alongside Section 25 - overlaps G7's existing "consume canonical sections" rule; future agents need to remember two rules; preferred path is single G7 with extended scope per DECISION_LOG. (c) Bundle with Cost Discipline (D-2026-05-03-04) - conflates two distinct concerns; cost-discipline is a Stage 0 operating rule, security governance is a route-construction control framework; landing them as separate waves preserves clean per-decision DECISION_LOG entries. (d) Defer Section 25 until Step 4 - leaves 3D to invent security controls inline; loses the locking benefit before the larger surface ships. (e) Hardcode exact vendor free-tier capabilities, prices, quotas, or plan features in MASTER - prices and free-tier limits change frequently; would require recurring documentation churn for no proportionate value; per G9, vendor capabilities verified from official sources at decision time and named vendors in 25.11 remain example-only. (f) Force Cloudflare or any specific WAF / DNS / rate-limit vendor in 25.11.4 - would lock a vendor decision; corrected to "evaluate a free DNS / WAF / rate-limit option such as Cloudflare free tier or equivalent before friendly pilot", with explicit note that fronting Netlify with such a service typically requires a custom domain and cannot be applied to the `netlify.app` subdomain. (g) Treat Platform Ownership Register population as a Stage 0 requirement - over-formalises POC stage; correct trigger is friendly pilot per 25.13.
- **Impact**: New `MASTER_PROJECT.md` Section 25 (14 subsections, ~280 lines) captures all locked content. `MASTER_PROJECT.md` Section 0 metadata refreshed: Document version v2.1 → v2.2; Last-meaningful-update line refreshed to "2026-05-03 (post External Threat Security & Platform Hardening Guardrails, D-2026-05-03-05)". `CURRENT_STATUS.md` Repo Health gains a new bullet pointing 3D and onward route work at Section 25 alongside Section 23; CURRENT_STATUS Last-updated header refreshed to also cite C-2026-05-03-05. New `CHANGE_LOG.md` entry C-2026-05-03-05. **`AGENTS.md` is NOT changed** — G7 already requires canonical-section consumption; this DECISION_LOG entry extends G7's effective scope to include Section 25 alongside Section 23, avoiding rule overlap and keeping the working-rules surface lean. Section 14 backend execution sequence untouched. The 3D plan (still on hold) gets refreshed in its next plan turn to cite Section 25 alongside Section 23 for every guardrail it consumes; Decisions G / H / I are now locked at the Section 25 level rather than open at the 3D plan level. Net new Stage 0 spend: zero. First paid item triggers at friendly pilot (Section 25.11.8 transactional email for branded password reset sender). Future entity route groups (3E Team, 3F Modules, Step 4 Auth, Step 5 Persistence) consume Section 25 alongside Section 23 per the consumption rule in 25.14.

## D-2026-05-04-01 - Section 14 Step 3D plan decisions A through F selected

- **Decision**: Lock the six open decisions from the revised 3D plan as follows. **(A)** Permission action codes for reopen and cancel: A1 — add distinct `Action.TASK_REOPEN` and `Action.TASK_CANCEL` codes to `permissions.ts`, but only when introduced by 3D-3 (lifecycle actions sub-wave); 3D-1 does NOT add them. **(B)** Read endpoint permission: B1 — add `Action.TASK_VIEW` (added in 3D-1 to support GET endpoints). **(C)** ARTICLE_STAFF read scope: C1 — server-enforced filter restricting ARTICLE_STAFF to tasks where they are creator OR assignee. **(D)** Hydration depth on Task responses: D1 — raw IDs only (no relation hydration beyond `assignees: { userId }`); UI hydrates names from existing endpoints when needed. **(E)** GET filter set: E1 — lean MVP `page, pageSize, status, priority, clientId, reviewerId, from, to`; `assigneeId` and free-text search deferred. **(F)** PATCH semantics for `assigneeIds`: F1 — reject `assigneeIds` in PATCH `/api/tasks/[id]`; assignee mutation goes through the dedicated `PATCH /api/tasks/[id]/assignees` endpoint that ships in 3D-2. Decisions G / H / I were already locked at MASTER Section 25 (length caps; Zod `.strict()`; ActivityLog metadataJson policy) and consumed as constraints rather than decided here.
- **Why**: Each "(Strongly recommended)" default in the 3D plan follows an established pattern from 3B (clients) or 3C (activity), or directly consumes Section 23 / Section 25. A1 mirrors 3C's `ACTIVITY_VIEW` precedent for distinct action codes per concern. B1 mirrors `CLIENT_VIEW` and `ACTIVITY_VIEW`. C1 is the canonical "own + assigned" scope from Section 10 "View reports = own". D1 mirrors 3B and 3C raw-ID responses; saves payload size and avoids N+1 risk. E1 covers Phase 1 reporting needs (Section 25.12.A concerns 6, 7, 8 about visibility / control / reliability) without indexed-search complexity. F1 keeps assignee mutation on a single dedicated route (one ActivityLog event chain; one place for set-semantics enforcement). Locking all six together at 3D-1 commit means the 3D-2 and 3D-3 plan turns consume settled decisions rather than re-opening them.
- **Alternatives rejected**: Decision-by-decision alternatives are documented in the revised 3D plan. Common rejections: B2 (reuse `TASK_ADD_NOTE` as proxy for read) — conflates concepts; C2 (firm-wide read for ARTICLE_STAFF) — too permissive given Section 10; D2 / D3 (relation hydration) — payload bloat + N+1 risk; E2 / E3 (wider filters / free-text search) — deferred to Phase 2 to keep the surface lean; F2 (allow `assigneeIds` in PATCH as replace-all) — creates two paths for assignee mutation and conflicts with set-semantics design.
- **Impact**: 3D-1 implementation (this wave) creates `src/lib/task-constants.ts` with `TASK_STATUSES` (7-value canonical set, no `REOPENED`), `PRIORITIES` (4-value), `TASK_STATUS_TRANSITIONS` map (per Section 23.2; `CLOSED → IN_PROGRESS` only via reopen action endpoint that ships in 3D-3), length caps (`MAX_TASK_TITLE_LENGTH = 200`, `MAX_TASK_DESCRIPTION_LENGTH = 4000`, `MAX_TASK_NOTE_LENGTH = 4000`, `MAX_ASSIGNEES_PER_TASK = 50`, `MAX_TASK_PAGE_SIZE = 200`), and `isAllowedTransition()` helper. `src/lib/permissions.ts` gains `Action.TASK_VIEW` added to FIRM_ADMIN, PARTNER, MANAGER, and ARTICLE_STAFF arrays (ARTICLE_STAFF route-layer scope per C1 enforces own-or-assigned only). `Action.TASK_REOPEN` and `Action.TASK_CANCEL` per A1 are NOT added in 3D-1; they ship with 3D-3. `src/app/api/tasks/route.ts` (NEW) implements GET list + POST create per E1 / D1. `src/app/api/tasks/[id]/route.ts` (NEW) implements GET single only — PATCH ships in 3D-2 per F1's separation. `writeActivityLog()` is called at `TASK_CREATE` semantic point but remains the deferred no-op stub from 3A (D-2026-04-30-15 Decision 4). No schema change. No new spend. Locked-by-default 401 contract preserved. Section 14 sequence untouched. 3D-2 (mutations) and 3D-3 (lifecycle actions) follow as separate Plan → Approval → Execution waves.

## D-2026-05-04-02 - Section 14 Step 3D-2 plan: J / K / L decisions and corrected PATCH design

- **Decision**: Lock the three open decisions from the 3D-2 plan and the corrected PATCH design that emerged from the plan review.
  - **J1**: `clientId` is immutable via PATCH `/api/tasks/[id]`. Tasks are created against a specific client; client reassignment is unusual and should be modelled as a new task. Zod `.strict()` rejects `clientId` in PATCH bodies with 422.
  - **K1**: `isOwnTask = isCreator OR isAssignee` for the `TASK_MOVE_TO_REVIEW` context check. Mirrors GET visibility scope per Decision C1; consistent "own" semantics across the codebase.
  - **L1**: Reviewer change requires only `Action.TASK_EDIT` permission and active `FirmMember` validation. No additional constraints (reviewer may be the task creator, an assignee, or any other active `FirmMember` of the firm).
  - **Corrected PATCH permission flow**: `requireAuth(Action.TASK_VIEW)` is the FIRST gate but ONLY establishes authenticated route entry. It does NOT authorize mutation. After body parse and task lookup, an operation classifier dispatches to the appropriate mutation authorization: any non-status field edit (with or without status) requires `TASK_EDIT`; status-only move to `UNDER_REVIEW` uses `TASK_MOVE_TO_REVIEW` with `{ isOwnTask }`; status-only move to non-`UNDER_REVIEW` requires `TASK_EDIT`. This pattern allows ARTICLE_STAFF to move own / assigned tasks to `UNDER_REVIEW` (the only PATCH path they have) without granting them blanket `TASK_EDIT` permission.
  - **Corrected PATCH note semantics**: `note` in PATCH body is allowed only when `status` is being changed in the same PATCH. Note-only PATCH or non-status field edit with note → 422 with redirect "Use POST /api/tasks/[id]/notes". Note required when status moves to `PENDING_CLIENT`, `PENDING_INTERNAL`, or `UNDER_REVIEW` per Section 23.5; optional for `IN_PROGRESS` revert paths. This tightens Section 23.5's "optional on field-only PATCH" by removing the option entirely - field edits without status changes go through the dedicated `/notes` endpoint.
- **Why**: The original 3D-2 plan had a permission-gate contradiction: `requireAuth(TASK_EDIT)` as first gate would 403 ARTICLE_STAFF before any `TASK_MOVE_TO_REVIEW` context check could fire. The corrected flow uses `TASK_VIEW` for auth-entry only, then dispatches mutation authorization based on the operation classifier. This is the canonical pattern for routes that have heterogeneous mutation permissions on the same endpoint. The note-semantic tightening removes ambiguity about when notes are appropriate in PATCH bodies — they belong to status changes only; field edits without status changes go through the dedicated `/notes` endpoint. J1 / K1 / L1 follow established 3B / 3C / 3D-1 patterns and Section 10 / Section 23 / Section 25 contracts.
- **Alternatives rejected**: J2 (allow `clientId` mutation) — unusual workflow, safer to model as a new task. J3 (defer to future "transfer" capability) — no clear use case yet. K2 (`isOwnTask = isAssignee only`) — diverges from GET visibility scope per Decision C1. K3 (`isOwnTask = isAssignee AND isCreator`) — overly restrictive. L2 (reviewer cannot be assignee) — workflow opinion not supported by Section 23. L3 (reviewer cannot be creator) — same. L4 (combination) — same. Original PATCH first-gate design with `TASK_EDIT` — broken for ARTICLE_STAFF move-to-review; rejected as the permission-gate contradiction. Adding a new `Action.TASK_PATCH` constant — overlaps with existing `TASK_EDIT` and `TASK_MOVE_TO_REVIEW` actions and creates ambiguity. Allowing note in PATCH without status (looser than the corrected design) — creates ambiguity and competes with the dedicated `/notes` endpoint.
- **Impact**: 3D-2 implementation (this wave) consumes J1 / K1 / L1 / corrected flow / corrected note semantics as locked constraints. `src/app/api/tasks/[id]/route.ts` is rewritten with GET preserved unchanged from 3D-1 and PATCH added implementing the operation classifier and the dispatched permission checks. `src/app/api/tasks/[id]/notes/route.ts` (NEW) implements POST status-less note. `src/app/api/tasks/[id]/assignees/route.ts` (NEW) implements PATCH set-semantics with route-layer ARTICLE_STAFF rejection per Section 23.5 and `requirePermission(TASK_EDIT)` dispatch per the corrected flow. No changes to `src/lib/permissions.ts` (existing `TASK_VIEW`, `TASK_EDIT`, `TASK_MOVE_TO_REVIEW`, `TASK_ADD_NOTE` actions cover all 3D-2 needs). No changes to `src/lib/task-constants.ts` (existing `TASK_STATUS_TRANSITIONS`, length caps, `isAllowedTransition()` helper cover 3D-2). No schema changes. `writeActivityLog()` remains the deferred no-op stub from 3A; routes call it at canonical action strings (`TASK_STATUS_CHANGE`, `TASK_REVIEWER_CHANGE`, `TASK_UPDATE`, `TASK_NOTE_ADD`, `TASK_ASSIGNEE_ADD`, `TASK_ASSIGNEE_REMOVE`) per Section 23.6 / Section 25 / Decision I. Net new Stage 0 spend: zero. Locked-by-default 401 contract preserved. Section 14 sequence untouched. 3D-3 (close / reopen / cancel) follows as a separate Plan → Approval → Execution wave; `Action.TASK_REOPEN` and `Action.TASK_CANCEL` per Decision A1 ship in 3D-3.
